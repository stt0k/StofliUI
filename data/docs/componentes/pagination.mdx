---
title: "Pagination"
description: >-
  Un componente de paginación para navegar a través de contenido dividido en páginas.
---

<div className="mt-10 space-y-8">
  <div>
    <HeadingAnchor id="instalacion">Instalación</HeadingAnchor>

```bash
# Instalar dependencias necesarias
npm install framer-motion lucide-react clsx tailwind-merge

# Instalar el componente
npx @stofli/ui add pagination
```

Si no tienes la utilidad `cn`, necesitarás crear un archivo `lib/utils.ts` con el siguiente contenido:

```tsx
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

  </div>

  <div>
    <HeadingAnchor id="uso">Uso</HeadingAnchor>

```tsx
import { Pagination } from "@stofli/ui";

export default function App() {
  return <Pagination totalPages={10} currentPage={1} />;
}
```

  </div>

  <div>
    <HeadingAnchor id="ejemplos">Ejemplos</HeadingAnchor>

    <div className="mt-4">
      <HeadingAnchor id="basico" level={3} >Básico</HeadingAnchor>

Un componente de paginación básico con números de página y flechas de navegación.

<DemoTabs
  preview={
    <div className="w-full flex justify-center">
      <Pagination totalPages={10} currentPage={1} />
    </div>
  }
>
```tsx
import { Pagination } from "@stofli/ui";

export default function PaginationDemo() {
  return (
    <Pagination
      totalPages={10}
      currentPage={1}
    />
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="variantes" level={3} >Variantes</HeadingAnchor>

El componente de paginación está disponible en diferentes variantes de color.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4 w-full">
      <Pagination totalPages={5} variant="default" />
      <Pagination totalPages={5} variant="primary" />
      <Pagination totalPages={5} variant="secondary" />
      <Pagination totalPages={5} variant="success" />
      <Pagination totalPages={5} variant="warning" />
      <Pagination totalPages={5} variant="danger" />
    </div>
  }
>
```tsx
import { Pagination } from "@stofli/ui";

export default function PaginationVariantsDemo() {
  return (
    <div className="flex flex-col gap-4 w-full">
      <Pagination totalPages={5} variant="default" />
      <Pagination totalPages={5} variant="primary" />
      <Pagination totalPages={5} variant="secondary" />
      <Pagination totalPages={5} variant="success" />
      <Pagination totalPages={5} variant="warning" />
      <Pagination totalPages={5} variant="danger" />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="tamanos" level={3} >Tamaños</HeadingAnchor>

El componente de paginación está disponible en tres tamaños: sm, md y lg.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4 w-full">
      <Pagination totalPages={5} size="sm" />
      <Pagination totalPages={5} size="md" />
      <Pagination totalPages={5} size="lg" />
    </div>
  }
>
```tsx
import { Pagination } from "@stofli/ui";

export default function PaginationSizesDemo() {
  return (
    <div className="flex flex-col gap-4 w-full">
      <Pagination totalPages={5} size="sm" />
      <Pagination totalPages={5} size="md" />
      <Pagination totalPages={5} size="lg" />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="con-botones-primera-ultima" level={3} >Con botones de primera y última página</HeadingAnchor>

Usando `withEdges={true}`, muestra botones adicionales para ir a la primera y última página.

<DemoTabs
  preview={
    <div className="w-full flex justify-center">
      <Pagination totalPages={10} withEdges />
    </div>
  }
>
```tsx
import { Pagination } from "@stofli/ui";

export default function PaginationWithEdgesDemo() {
  return (
    <Pagination totalPages={10} withEdges />
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="con-texto-botones" level={3} >Con texto en los botones</HeadingAnchor>

Usando `withText={true}`, añade texto descriptivo a los botones de navegación.

<DemoTabs
  preview={
    <div className="w-full flex justify-center">
      <Pagination totalPages={10} withText />
    </div>
  }
>
```tsx
import { Pagination } from "@stofli/ui";

export default function PaginationWithTextDemo() {
  return (
    <Pagination totalPages={10} withText />
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="texto-personalizado-botones" level={3} >Con texto personalizado en los botones</HeadingAnchor>

Puedes personalizar el texto que aparece en los botones de navegación usando la propiedad `customText`.

<DemoTabs
  preview={
    <div className="w-full flex justify-center">
      <Pagination 
        totalPages={10} 
        withText 
        withEdges
        customText={{
          previous: "Atrás",
          next: "Adelante",
          first: "Inicio",
          last: "Final"
        }}
      />
    </div>
  }
>
```tsx
import { Pagination } from "@stofli/ui";

export default function PaginationCustomTextDemo() {
  return (
    <Pagination 
      totalPages={10} 
      withText 
      withEdges
      customText={{
        previous: "Atrás",
        next: "Adelante",
        first: "Inicio",
        last: "Final"
      }}
    />
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="solo-boton-siguiente-anterior" level={3} >Mostrar solo botón siguiente o anterior</HeadingAnchor>

Usando las propiedades `showPrevious` y `showNext`, puedes controlar qué botones de navegación se muestran. Esto es útil para la primera o última página.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4 w-full">
      <div className="border border-dashed border-zinc-300 dark:border-zinc-700 p-4">
        <Pagination 
          totalPages={10} 
          withNumbers={false}
          fullWidth 
          withText
          showPrevious={false}
          showNext={true}
          customText={{ next: "Siguiente página" }}
        />
      </div>
      <div className="border border-dashed border-zinc-300 dark:border-zinc-700 p-4">
        <Pagination 
          totalPages={10} 
          withNumbers={false}
          fullWidth 
          withText
          showPrevious={true}
          showNext={false}
          customText={{ previous: "Página anterior" }}
        />
      </div>
    </div>
  }
>
```tsx
import { Pagination } from "@stofli/ui";

export default function PaginationSingleButtonDemo() {
  return (
    <div className="flex flex-col gap-4 w-full">
      {/* Solo botón siguiente (útil para primera página) */}
      <div className="border border-dashed border-zinc-300 dark:border-zinc-700 p-4">
        <Pagination 
          totalPages={10} 
          withNumbers={false}
          fullWidth 
          withText
          showPrevious={false}
          showNext={true}
          customText={{ next: "Siguiente página" }}
        />
      </div>
      
      {/* Solo botón anterior (útil para última página) */}
      <div className="border border-dashed border-zinc-300 dark:border-zinc-700 p-4">
        <Pagination 
          totalPages={10} 
          withNumbers={false}
          fullWidth 
          withText
          showPrevious={true}
          showNext={false}
          customText={{ previous: "Página anterior" }}
        />
      </div>
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="ancho-completo" level={3} >Ancho completo</HeadingAnchor>

Usando `fullWidth={true}`, el componente ocupará todo el ancho disponible de su contenedor y aplicará un diseño con espacio entre los botones (justify-between).

<DemoTabs
  preview={
    <div className="w-full border border-dashed border-zinc-300 dark:border-zinc-700 p-4">
      <Pagination 
        totalPages={10} 
        fullWidth 
      />
    </div>
  }
>
```tsx
import { Pagination } from "@stofli/ui";

export default function PaginationFullWidthDemo() {
  return (
    <div className="w-full border border-dashed border-zinc-300 dark:border-zinc-700 p-4">
      <Pagination 
        totalPages={10} 
        fullWidth 
      />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="deshabilitado" level={3} >Deshabilitado</HeadingAnchor>

Un componente de paginación deshabilitado que no permite interacción.

<DemoTabs
  preview={
    <div className="w-full flex justify-center">
      <Pagination totalPages={10} disabled />
    </div>
  }
>
```tsx
import { Pagination } from "@stofli/ui";

export default function PaginationDisabledDemo() {
  return (
    <Pagination 
      totalPages={10} 
      disabled 
    />
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="clases-personalizadas" level={3} >Con clases personalizadas</HeadingAnchor>

Puedes personalizar cualquier parte del componente usando la propiedad `className` general o las propiedades específicas de classNames con clases de Tailwind CSS.

<DemoTabs
  preview={
    <div className="w-full flex justify-center">
      <Pagination 
        totalPages={10}
        className="bg-blue-50 dark:bg-blue-950/20 p-3 rounded-lg shadow-sm border border-blue-200 dark:border-blue-800" 
        variant="primary"
        navButtonClassName="bg-blue-100 dark:bg-blue-900/40 border-blue-300 dark:border-blue-700 rounded-lg hover:shadow-md transition-all duration-300"
        activePageClassName="font-bold"
        pageButtonClassName="rounded-lg hover:scale-110 transition-transform duration-200"
        indicatorClassName="shadow-md"
        numbersContainerClassName="gap-2"
      />
    </div>
  }
>
```tsx
import { Pagination } from "@stofli/ui";

export default function PaginationCustomClassDemo() {
  return (
    <Pagination 
      totalPages={10} 
      className="bg-blue-50 dark:bg-blue-950/20 p-3 rounded-lg shadow-sm border border-blue-200 dark:border-blue-800" 
      variant="primary"
      navButtonClassName="bg-blue-100 dark:bg-blue-900/40 border-blue-300 dark:border-blue-700 rounded-lg hover:shadow-md transition-all duration-300"
      activePageClassName="font-bold"
      pageButtonClassName="rounded-lg hover:scale-110 transition-transform duration-200"
      indicatorClassName="shadow-md"
      numbersContainerClassName="gap-2"
    />
  );
}
```
</DemoTabs>
    </div>
  </div>

  <div>
    <HeadingAnchor id="props">Props</HeadingAnchor>

| Prop                        | Tipo                                                                          | Default                                                                         | Descripción                                             |
| --------------------------- | ----------------------------------------------------------------------------- | ------------------------------------------------------------------------------- | ------------------------------------------------------- |
| `totalPages`                | `number`                                                                      | -                                                                               | Número total de páginas (requerido)                     |
| `currentPage`               | `number`                                                                      | `1`                                                                             | Página actual                                           |
| `onPageChange`              | `(page: number) => void`                                                      | -                                                                               | Función que se ejecuta al cambiar de página             |
| `siblingsCount`             | `number`                                                                      | `1`                                                                             | Número de páginas adyacentes a mostrar                  |
| `variant`                   | `"default" \| "primary" \| "secondary" \| "success" \| "warning" \| "danger"` | `"default"`                                                                     | Variante de color del componente                        |
| `size`                      | `"sm" \| "md" \| "lg"`                                                        | `"md"`                                                                          | Tamaño del componente                                   |
| `withNumbers`               | `boolean`                                                                     | `true`                                                                          | Mostrar números de página                               |
| `withEdges`                 | `boolean`                                                                     | `false`                                                                         | Mostrar botones para ir a la primera y última página    |
| `withText`                  | `boolean`                                                                     | `false`                                                                         | Mostrar texto en los botones de navegación              |
| `disabled`                  | `boolean`                                                                     | `false`                                                                         | Deshabilitar el componente                              |
| `className`                 | `string`                                                                      | -                                                                               | Clases adicionales para el componente principal         |
| `fullWidth`                 | `boolean`                                                                     | `false`                                                                         | Ocupar todo el ancho disponible y aplicar space-between |
| `customText`                | `object`                                                                      | `{ previous: "Anterior", next: "Siguiente", first: "Primera", last: "Última" }` | Personalizar el texto de los botones de navegación      |
| `showPrevious`              | `boolean`                                                                     | `true`                                                                          | Mostrar el botón de navegación hacia atrás              |
| `showNext`                  | `boolean`                                                                     | `true`                                                                          | Mostrar el botón de navegación hacia adelante           |
| `navButtonClassName`        | `string`                                                                      | -                                                                               | Clases para los botones de navegación                   |
| `activePageClassName`       | `string`                                                                      | -                                                                               | Clases para la página activa seleccionada               |
| `pageButtonClassName`       | `string`                                                                      | -                                                                               | Clases para los botones de página                       |
| `ellipsisClassName`         | `string`                                                                      | -                                                                               | Clases para los elementos de elipsis                    |
| `indicatorClassName`        | `string`                                                                      | -                                                                               | Clases para el indicador animado                        |
| `numbersContainerClassName` | `string`                                                                      | -                                                                               | Clases para el contenedor de números de página          |

  </div>
</div>

export const toc = [
  { title: "Instalación", href: "#instalacion" },
  { title: "Uso", href: "#uso" },
  {
    title: "Ejemplos",
    href: "#ejemplos",
    children: [
      { title: "Básico", href: "#basico" },
      { title: "Variantes", href: "#variantes" },
      { title: "Tamaños", href: "#tamanos" },
      {
        title: "Con botones de primera y última página",
        href: "#con-botones-primera-ultima",
      },
      { title: "Con texto en los botones", href: "#con-texto-botones" },
      {
        title: "Con texto personalizado en los botones",
        href: "#texto-personalizado-botones",
      },
      {
        title: "Mostrar solo botón siguiente o anterior",
        href: "#solo-boton-siguiente-anterior",
      },
      { title: "Ancho completo", href: "#ancho-completo" },
      { title: "Deshabilitado", href: "#deshabilitado" },
      { title: "Con clases personalizadas", href: "#clases-personalizadas" },
    ],
  },
  { title: "Props", href: "#props" },
];
