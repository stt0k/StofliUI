---
title: "Checkbox"
description: "Un componente de checkbox moderno con animaciones suaves y diferentes variantes de estilo."
---

## Instalación

```bash
# Instalar dependencias necesarias
npm install framer-motion

# Instalar el componente
npx @stofli/ui add checkbox
```

## Uso

```tsx
import { Checkbox } from "@stofli/ui";

export default function App() {
  return (
    <Checkbox
      label="Acepto los términos y condiciones"
      onChange={(checked) => console.log("Checked:", checked)}
    />
  );
}
```

## Ejemplos

### Básico

Muestra cuatro estados del checkbox: normal, marcado, deshabilitado y requerido.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4">
      <Checkbox label="Checkbox básico" />
      <Checkbox label="Checkbox marcado por defecto" defaultChecked />
      <Checkbox label="Checkbox deshabilitado" disabled />
      <Checkbox label="Checkbox requerido" required />
    </div>
  }
>
```tsx
import { Checkbox } from "@stofli/ui";

export default function CheckboxDemo() {
  return (
    <div className="flex flex-col gap-4">
      <Checkbox label="Checkbox básico" />
      <Checkbox label="Checkbox marcado por defecto" defaultChecked />
      <Checkbox label="Checkbox deshabilitado" disabled />
      <Checkbox label="Checkbox requerido" required />
    </div>
  );
}
```
</DemoTabs>

### Tamaños

Usando la prop `size`: sm, md y lg.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4">
      <Checkbox size="sm" label="Checkbox pequeño" />
      <Checkbox size="md" label="Checkbox mediano" />
      <Checkbox size="lg" label="Checkbox grande" />
    </div>
  }
>
```tsx
import { Checkbox } from "@stofli/ui";

export default function CheckboxSizesDemo() {
  return (
    <div className="flex flex-col gap-4">
      <Checkbox size="sm" label="Checkbox pequeño" />
      <Checkbox size="md" label="Checkbox mediano" />
      <Checkbox size="lg" label="Checkbox grande" />
    </div>
  );
}
```
</DemoTabs>

### Variantes

Usando la prop `variant`: default, primary, secondary, success, warning y danger.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4">
      <Checkbox variant="default" label="Variante Default" defaultChecked />
      <Checkbox variant="primary" label="Variante Primary" defaultChecked />
      <Checkbox variant="secondary" label="Variante Secondary" defaultChecked />
      <Checkbox variant="success" label="Variante Success" defaultChecked />
      <Checkbox variant="warning" label="Variante Warning" defaultChecked />
      <Checkbox variant="danger" label="Variante Danger" defaultChecked />
    </div>
  }
>
```tsx
import { Checkbox } from "@stofli/ui";

export default function CheckboxVariantsDemo() {
  return (
    <div className="flex flex-col gap-4">
      <Checkbox variant="default" label="Variante Default" defaultChecked />
      <Checkbox variant="primary" label="Variante Primary" defaultChecked />
      <Checkbox variant="secondary" label="Variante Secondary" defaultChecked />
      <Checkbox variant="success" label="Variante Success" defaultChecked />
      <Checkbox variant="warning" label="Variante Warning" defaultChecked />
      <Checkbox variant="danger" label="Variante Danger" defaultChecked />
    </div>
  );
}
```
</DemoTabs>

### Radios

Usando la prop `radius`: none, sm, md y full.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4">
      <Checkbox 
        radius="none" 
        label="Sin radio" 
        defaultChecked 
        variant="primary"
      />
      <Checkbox 
        radius="sm" 
        label="Radio pequeño" 
        defaultChecked 
        variant="primary"
      />
      <Checkbox 
        radius="md" 
        label="Radio mediano" 
        defaultChecked 
        variant="primary"
      />
      <Checkbox 
        radius="full" 
        label="Radio completo" 
        defaultChecked 
        variant="primary"
      />
    </div>
  }
>
```tsx
import { Checkbox } from "@stofli/ui";

export default function CheckboxRadiusDemo() {
  return (
    <div className="flex flex-col gap-4">
      <Checkbox 
        radius="none" 
        label="Sin radio" 
        defaultChecked 
        variant="primary"
      />
      <Checkbox 
        radius="sm" 
        label="Radio pequeño" 
        defaultChecked 
        variant="primary"
      />
      <Checkbox 
        radius="md" 
        label="Radio mediano" 
        defaultChecked 
        variant="primary"
      />
      <Checkbox 
        radius="full" 
        label="Radio completo" 
        defaultChecked 
        variant="primary"
      />
    </div>
  );
}
```
</DemoTabs>

## Props

| Prop             | Tipo                                                                          | Default     | Descripción                          |
| ---------------- | ----------------------------------------------------------------------------- | ----------- | ------------------------------------ |
| `checked`        | `boolean`                                                                     | -           | Estado controlado del checkbox       |
| `defaultChecked` | `boolean`                                                                     | `false`     | Estado inicial del checkbox          |
| `disabled`       | `boolean`                                                                     | `false`     | Deshabilita el checkbox              |
| `required`       | `boolean`                                                                     | `false`     | Marca el checkbox como requerido     |
| `label`          | `string`                                                                      | -           | Texto de la etiqueta                 |
| `name`           | `string`                                                                      | -           | Nombre del campo para formularios    |
| `value`          | `string`                                                                      | -           | Valor del checkbox                   |
| `size`           | `"sm" \| "md" \| "lg"`                                                        | `"md"`      | Tamaño del checkbox                  |
| `variant`        | `"default" \| "primary" \| "secondary" \| "success" \| "warning" \| "danger"` | `"default"` | Variante de estilo                   |
| `radius`         | `"none" \| "sm" \| "md" \| "full"`                                            | `"md"`      | Radio de las esquinas                |
| `className`      | `string`                                                                      | -           | Clases adicionales                   |
| `onChange`       | `(checked: boolean) => void`                                                  | -           | Función llamada al cambiar el estado |
