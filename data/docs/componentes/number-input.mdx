---
title: "Number Input"
description: >-
  Un componente para entrada de valores numéricos con controles de incremento/decremento y animaciones.
---

<div className="mt-10 space-y-8">
  <div>
    <HeadingAnchor id="import">Import</HeadingAnchor>

```tsx
import { NumberInput } from "stofli-ui"
```

  </div>

  
  <div>
    <HeadingAnchor id="uso">Uso</HeadingAnchor>

Muestra un input numérico con un valor predeterminado de 10, un rango mínimo de 0 y un rango máximo de 100.
<DemoTabs
  preview={
    <div className="flex flex-col gap-8">
      <NumberInput label="Cantidad" defaultValue={10} min={0} max={100} />
    </div>
  }
>
```tsx
import { NumberInput } from "stofli-ui";

export default function NumberInputDemo() {
  return <NumberInput label="Cantidad" defaultValue={10} min={0} max={100} />;
}
```
</DemoTabs>

    <div className="mt-6">
      <HeadingAnchor id="variantes" level={3} >Variantes</HeadingAnchor>

El componente NumberInput ofrece distintas variantes para adaptarse a diferentes contextos visuales.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4">
      <NumberInput variant="default" label="Default" defaultValue={5} />
      <NumberInput variant="primary" label="Primary" defaultValue={5} />
      <NumberInput variant="secondary" label="Secondary" defaultValue={5} />
      <NumberInput variant="success" label="Success" defaultValue={5} />
      <NumberInput variant="warning" label="Warning" defaultValue={5} />
      <NumberInput variant="danger" label="Danger" defaultValue={5} />
    </div>
  }
>
```tsx
import { NumberInput } from "stofli-ui";

export default function NumberInputVariantsDemo() {
  return (
    <div className="flex flex-col gap-4">
      <NumberInput variant="default" label="Default" defaultValue={5} />
      <NumberInput variant="primary" label="Primary" defaultValue={5} />
      <NumberInput variant="secondary" label="Secondary" defaultValue={5} />
      <NumberInput variant="success" label="Success" defaultValue={5} />
      <NumberInput variant="warning" label="Warning" defaultValue={5} />
      <NumberInput variant="danger" label="Danger" defaultValue={5} />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="tamanos" level={3} >Tamaños</HeadingAnchor>

Puedes elegir entre diferentes tamaños para el input numérico.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4">
      <NumberInput size="sm" label="Pequeño" defaultValue={5} />
      <NumberInput size="md" label="Mediano" defaultValue={5} />
      <NumberInput size="lg" label="Grande" defaultValue={5} />
    </div>
  }
>
```tsx
import { NumberInput } from "stofli-ui";

export default function NumberInputSizesDemo() {
  return (
    <div className="flex flex-col gap-4">
      <NumberInput size="sm" label="Pequeño" defaultValue={5} />
      <NumberInput size="md" label="Mediano" defaultValue={5} />
      <NumberInput size="lg" label="Grande" defaultValue={5} />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="radios-de-borde" level={3} >Radios de borde</HeadingAnchor>

Puedes personalizar el radio de los bordes del input para adaptarlo a tu diseño.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4">
      <NumberInput 
        label="Sin bordes redondeados" 
        radius="none"
        defaultValue={5}
        variant="primary"
      />
      <NumberInput 
        label="Radio pequeño"
        radius="sm"
        defaultValue={5}
        variant="secondary"
      />
      <NumberInput 
        label="Radio mediano (predeterminado)"
        radius="md"
        defaultValue={5}
        variant="success"
      />
      <NumberInput 
        label="Radio grande"
        radius="full"
        defaultValue={5}
        variant="warning"
      />
    </div>
  }
>
```tsx
import { NumberInput } from "stofli-ui";

export default function NumberInputRadiusDemo() {
  return (
    <div className="flex flex-col gap-4">
      <NumberInput 
        label="Sin bordes redondeados" 
        radius="none"
        defaultValue={5}
        variant="primary"
      />
      <NumberInput 
        label="Radio pequeño"
        radius="sm"
        defaultValue={5}
        variant="secondary"
      />
      <NumberInput 
        label="Radio mediano (predeterminado)"
        radius="md"
        defaultValue={5}
        variant="success"
      />
      <NumberInput 
        label="Radio grande"
        radius="full"
        defaultValue={5}
        variant="warning"
      />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="posicion-controles" level={3} >Posición de los controles</HeadingAnchor>

Puedes elegir diferentes posiciones para los controles de incremento/decremento.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4">
      <NumberInput controlsPosition="right" label="Controles a la derecha" defaultValue={5} />
      <NumberInput controlsPosition="both" label="Controles en ambos lados" defaultValue={5} />
    </div>
  }
>
```tsx
import { NumberInput } from "stofli-ui";

export default function NumberInputControlsPositionDemo() {
  return (
    <div className="flex flex-col gap-4">
      <NumberInput controlsPosition="right" label="Controles a la derecha" defaultValue={5} />
      <NumberInput controlsPosition="both" label="Controles en ambos lados" defaultValue={5} />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="layout-controles" level={3} >Layout de controles</HeadingAnchor>

El componente ofrece diferentes estilos de layout para los controles.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4">
      <NumberInput controlsLayout="default" label="Layout predeterminado" defaultValue={5} />
      <NumberInput controlsLayout="compact" label="Layout compacto" defaultValue={5} />
    </div>
  }
>
```tsx
import { NumberInput } from "stofli-ui";

export default function NumberInputControlsLayoutDemo() {
  return (
    <div className="flex flex-col gap-4">
      <NumberInput controlsLayout="default" label="Layout predeterminado" defaultValue={5} />
      <NumberInput controlsLayout="compact" label="Layout compacto" defaultValue={5} />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="conjunto-iconos" level={3} >Conjunto de iconos</HeadingAnchor>

Puedes elegir entre diferentes estilos de iconos para los controles.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4">
      <NumberInput iconSet="chevron" label="Iconos chevron" defaultValue={5} />
      <NumberInput iconSet="plusminus" label="Iconos plus/minus" defaultValue={5} />
    </div>
  }
>
```tsx
import { NumberInput } from "stofli-ui";

export default function NumberInputIconSetsDemo() {
  return (
    <div className="flex flex-col gap-4">
      <NumberInput iconSet="chevron" label="Iconos chevron" defaultValue={5} />
      <NumberInput iconSet="plusminus" label="Iconos plus/minus" defaultValue={5} />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="estilos-animacion" level={3} >Estilos de animación</HeadingAnchor>

El componente incluye diferentes estilos de animación cuando el valor cambia.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4">
      <NumberInput animationStyle="slide" label="Animación deslizar" defaultValue={5} />
      <NumberInput animationStyle="pulse" label="Animación pulso" defaultValue={5} />
      <NumberInput animationStyle="scale" label="Animación escala" defaultValue={5} />
      <NumberInput animationStyle="bounce" label="Animación rebote" defaultValue={5} />
    </div>
  }
>
```tsx
import { NumberInput } from "stofli-ui";

export default function NumberInputAnimationsDemo() {
  return (
    <div className="flex flex-col gap-4">
      <NumberInput animationStyle="slide" label="Animación deslizar" defaultValue={5} />
      <NumberInput animationStyle="pulse" label="Animación pulso" defaultValue={5} />
      <NumberInput animationStyle="scale" label="Animación escala" defaultValue={5} />
      <NumberInput animationStyle="bounce" label="Animación rebote" defaultValue={5} />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="limites-paso" level={3} >Con límites y paso</HeadingAnchor>

Puedes establecer valores mínimos y máximos, así como el paso de incremento/decremento.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4">
      <NumberInput 
        label="Temperatura (°C)" 
        defaultValue={25} 
        min={-20} 
        max={50} 
        step={0.5} 
      />
    </div>
  }
>
```tsx
import { NumberInput } from "stofli-ui";

export default function NumberInputLimitsDemo() {
  return (
    <NumberInput 
      label="Temperatura (°C)" 
      defaultValue={25} 
      min={-20} 
      max={50} 
      step={0.5} 
    />
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="deshabilitado-solo-lectura" level={3} >Deshabilitado y solo lectura</HeadingAnchor>

<DemoTabs
  preview={
    <div className="flex flex-col gap-4">
      <NumberInput 
        label="Deshabilitado" 
        defaultValue={42} 
        disabled 
      />
      <NumberInput 
        label="Solo lectura" 
        defaultValue={42} 
        readonly 
      />
    </div>
  }
>
```tsx
import { NumberInput } from "stofli-ui";

export default function NumberInputDisabledDemo() {
  return (
    <div className="flex flex-col gap-4">
      <NumberInput 
        label="Deshabilitado" 
        defaultValue={42} 
        disabled 
      />
      <NumberInput 
        label="Solo lectura" 
        defaultValue={42} 
        readonly 
      />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="mensaje-error" level={3} >Con mensaje de error</HeadingAnchor>

Muestra un mensaje de error cuando se cumple una condición, en este caso, cuando el valor es negativo.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4">
      <NumberInput 
        label="Precio" 
        defaultValue={-10} 
        error={-10 < 0 ? "El precio no puede ser negativo" : undefined} 
      />
    </div>
  }
>
```tsx
import { NumberInput } from "stofli-ui";
import { useState } from "react";

export default function NumberInputErrorDemo() {
  const [value, setValue] = useState(-10);
  
  return (
    <NumberInput 
      label="Precio" 
      value={value}
      onChange={setValue}
      error={value < 0 ? "El precio no puede ser negativo" : undefined}
    />
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="ancho-completo" level={3} >Ancho completo</HeadingAnchor>

<DemoTabs
  preview={
    <div className="flex flex-col gap-4">
      <NumberInput 
        label="Cantidad" 
        defaultValue={5} 
        fullWidth 
      />
    </div>
  }
>
```tsx
import { NumberInput } from "stofli-ui";

export default function NumberInputFullWidthDemo() {
  return <NumberInput label="Cantidad" defaultValue={5} fullWidth />;
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="clases-personalizadas" level={3} >Con clases personalizadas</HeadingAnchor>

Puedes personalizar cada parte del componente usando las propiedades de className específicas para darle un aspecto único con Tailwind CSS.

<DemoTabs
  preview={
      <NumberInput 
        className="max-w-md"
        label="Personalizado" 
        labelClassName="text-blue-700 dark:text-blue-300 font-bold uppercase tracking-wider text-xs"
        inputClassName="border-indigo-300 dark:border-indigo-700 bg-indigo-50 dark:bg-indigo-950/30 text-indigo-700 dark:text-indigo-300 placeholder-indigo-400"
        variant="primary"
        defaultValue={5}
        min={0}
        max={10}
      />
  }
>
```tsx
import { NumberInput } from "stofli-ui";

export default function NumberInputCustomClassDemo() {
  return (
      <NumberInput 
        className="max-w-md"
        label="Personalizado" 
        labelClassName="text-blue-700 dark:text-blue-300 font-bold uppercase tracking-wider text-xs"
        inputClassName="border-indigo-300 dark:border-indigo-700 bg-indigo-50 dark:bg-indigo-950/30 text-indigo-700 dark:text-indigo-300 placeholder-indigo-400"
        variant="primary"
        defaultValue={5}
        min={0}
        max={10}
      />
  );
}
```
</DemoTabs>
    </div>
  </div>

  <div>
    <HeadingAnchor id="props">Props</HeadingAnchor>

| Prop                      | Tipo                                                                          | Valor por defecto | Descripción                                               |
| ------------------------- | ----------------------------------------------------------------------------- | ----------------- | --------------------------------------------------------- |
| `value`                   | `number`                                                                      | -                 | Valor controlado del input numérico                       |
| `defaultValue`            | `number`                                                                      | -                 | Valor inicial no controlado                               |
| `min`                     | `number`                                                                      | -                 | Valor mínimo permitido                                    |
| `max`                     | `number`                                                                      | -                 | Valor máximo permitido                                    |
| `step`                    | `number`                                                                      | `1`               | Incremento por cada pulsación en los controles            |
| `disabled`                | `boolean`                                                                     | `false`           | Deshabilita el input                                      |
| `readonly`                | `boolean`                                                                     | `false`           | Modo solo lectura                                         |
| `label`                   | `string`                                                                      | -                 | Etiqueta descriptiva para el input                        |
| `placeholder`             | `string`                                                                      | -                 | Texto de marcador de posición                             |
| `error`                   | `string`                                                                      | -                 | Mensaje de error a mostrar                                |
| `size`                    | `"sm" \| "md" \| "lg"`                                                        | `"md"`            | Tamaño del componente                                     |
| `variant`                 | `"default" \| "primary" \| "secondary" \| "success" \| "warning" \| "danger"` | `"default"`       | Variante de estilo                                        |
| `radius`                  | `"none" \| "sm" \| "md" \| "full"`                                            | `"md"`            | Radio de las esquinas                                     |
| `fullWidth`               | `boolean`                                                                     | `false`           | Ocupa el 100% del ancho disponible                        |
| `className`               | `string`                                                                      | -                 | Clases CSS personalizadas para el contenedor              |
| `inputClassName`          | `string`                                                                      | -                 | Clases CSS personalizadas para el input                   |
| `labelClassName`          | `string`                                                                      | -                 | Clases CSS personalizadas para la etiqueta                |
| `errorClassName`          | `string`                                                                      | -                 | Clases CSS personalizadas para el mensaje de error        |
| `controlsClassName`       | `string`                                                                      | -                 | Clases CSS personalizadas para el contenedor de controles |
| `controlButtonClassName`  | `string`                                                                      | -                 | Clases CSS personalizadas para los botones de control     |
| `inputContainerClassName` | `string`                                                                      | -                 | Clases CSS personalizadas para el contenedor del input    |
| `controlsLayout`          | `"default" \| "compact" \| "split"`                                           | `"default"`       | Diseño de los controles                                   |
| `controlsPosition`        | `"right" \| "both"`                                                           | `"right"`         | Posición de los controles                                 |
| `showControls`            | `boolean`                                                                     | `true`            | Muestra u oculta los controles                            |
| `iconSet`                 | `"chevron" \| "plusminus"`                                                    | `"chevron"`       | Conjunto de iconos a utilizar                             |
| `required`                | `boolean`                                                                     | `false`           | Marca el campo como obligatorio                           |
| `name`                    | `string`                                                                      | -                 | Nombre del campo (para formularios)                       |
| `id`                      | `string`                                                                      | -                 | ID único del elemento                                     |
| `animationStyle`          | `"slide" \| "pulse" \| "scale" \| "bounce"`                                   | `"slide"`         | Estilo de animación al cambiar valor                      |
| `format`                  | `"default" \| "percentage" \| "currency" \| "signed"`                         | `"default"`       | Formato de visualización del valor                        |
| `formatOptions`           | `{ symbol?: string; position?: "prefix" \| "suffix" }`                        | `{}`              | Opciones adicionales para el formato seleccionado         |
| `onChange`                | `(value: number) => void`                                                     | -                 | Función que se ejecuta al cambiar el valor                |
| `onFocus`                 | `(e: React.FocusEvent<HTMLInputElement>) => void`                             | -                 | Función que se ejecuta cuando el input recibe el foco     |
| `onBlur`                  | `(e: React.FocusEvent<HTMLInputElement>) => void`                             | -                 | Función que se ejecuta cuando el input pierde el foco     |
| `aria-label`              | `string`                                                                      | -                 | Etiqueta ARIA personalizada                               |
| `aria-describedby`        | `string`                                                                      | -                 | ID del elemento que describe el input                     |
| `description`             | `string`                                                                      | -                 | Texto descriptivo para el input                           |
| `errorId`                 | `string`                                                                      | -                 | ID personalizado para el mensaje de error                 |
| `descriptionId`           | `string`                                                                      | -                 | ID personalizado para la descripción                      |
| `hideLabel`               | `boolean`                                                                     | `false`           | Oculta visualmente la etiqueta pero la mantiene accesible |
| `autoComplete`            | `string`                                                                      | -                 | Valor para el atributo autoComplete del input             |

  </div>
</div>

export const toc = [
  { title: "Import", href: "#import" },
  {
    title: "Uso",
    href: "#uso",
    children: [
      { title: "Variantes", href: "#variantes" },
      { title: "Tamaños", href: "#tamanos" },
      { title: "Radios de borde", href: "#radios-de-borde" },
      { title: "Posición de los controles", href: "#posicion-controles" },
      { title: "Layout de controles", href: "#layout-controles" },
      { title: "Conjunto de iconos", href: "#conjunto-iconos" },
      { title: "Estilos de animación", href: "#estilos-animacion" },
      { title: "Con límites y paso", href: "#limites-paso" },
      {
        title: "Deshabilitado y solo lectura",
        href: "#deshabilitado-solo-lectura",
      },
      { title: "Con mensaje de error", href: "#mensaje-error" },
      { title: "Ancho completo", href: "#ancho-completo" },
      { title: "Con clases personalizadas", href: "#clases-personalizadas" },
    ],
  },
  { title: "Props", href: "#props" },
];
