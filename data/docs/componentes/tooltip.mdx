---
title: "Tooltip"
description: "Un componente para mostrar información adicional al pasar el ratón sobre un elemento."
---

## Instalación

```bash
# Instalar dependencias necesarias
npm install framer-motion

# Instalar el componente
npx @stofli/ui add tooltip
```

## Uso

```tsx
import { Tooltip, Button } from "@stofli/ui";

export default function App() {
  return (
    <Tooltip content="Esta es información adicional sobre el botón">
      <Button>Pasa el ratón sobre mí</Button>
    </Tooltip>
  );
}
```

## Ejemplos

### Básico

Un tooltip básico con posición arriba (por defecto).

<DemoTabs
  preview={
    <div className="flex flex-col items-center justify-center">
      <Tooltip content="Esta es información adicional sobre el botón">
        <Button>Pasa el ratón sobre mí</Button>
      </Tooltip>
    </div>
  }
>
```tsx
import { Tooltip, Button } from "@stofli/ui";

export default function TooltipDemo() {
  return (
    <Tooltip content="Esta es información adicional sobre el botón">
      <Button>Pasa el ratón sobre mí</Button>
    </Tooltip>
  );
}
```
</DemoTabs>

### Con flecha indicadora

Puedes añadir una flecha que apunte al elemento usando la prop `showArrow`.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-4 justify-center">
      <Tooltip 
        content="Tooltip con flecha arriba" 
        position="top"
        showArrow
      >
        <Button>Arriba</Button>
      </Tooltip>
      <Tooltip 
        content="Tooltip con flecha derecha" 
        position="right"
        showArrow
      >
        <Button>Derecha</Button>
      </Tooltip>
      <Tooltip 
        content="Tooltip con flecha abajo" 
        position="bottom"
        showArrow
      >
        <Button>Abajo</Button>
      </Tooltip>
      <Tooltip 
        content="Tooltip con flecha izquierda" 
        position="left"
        showArrow
      >
        <Button>Izquierda</Button>
      </Tooltip>
    </div>
  }
>
```tsx
import { Tooltip, Button } from "@stofli/ui";

export default function TooltipArrowDemo() {
  return (
    <div className="flex flex-wrap gap-4 justify-center">
      <Tooltip 
        content="Tooltip con flecha arriba" 
        position="top"
        showArrow
      >
        <Button>Arriba</Button>
      </Tooltip>
      <Tooltip 
        content="Tooltip con flecha derecha" 
        position="right"
        showArrow
      >
        <Button>Derecha</Button>
      </Tooltip>
      <Tooltip 
        content="Tooltip con flecha abajo" 
        position="bottom"
        showArrow
      >
        <Button>Abajo</Button>
      </Tooltip>
      <Tooltip 
        content="Tooltip con flecha izquierda" 
        position="left"
        showArrow
      >
        <Button>Izquierda</Button>
      </Tooltip>
    </div>
  );
}
```
</DemoTabs>

### Posiciones

El tooltip puede posicionarse en cuatro direcciones diferentes usando la prop `position`.

<DemoTabs
  preview={
    <div className="grid grid-cols-2 gap-8 p-8">
      <div className="flex justify-center">
        <Tooltip position="top" content="Posición arriba">
          <Button>Arriba</Button>
        </Tooltip>
      </div>
      <div className="flex justify-center">
        <Tooltip position="right" content="Posición derecha">
          <Button>Derecha</Button>
        </Tooltip>
      </div>
      <div className="flex justify-center">
        <Tooltip position="bottom" content="Posición abajo">
          <Button>Abajo</Button>
        </Tooltip>
      </div>
      <div className="flex justify-center">
        <Tooltip position="left" content="Posición izquierda">
          <Button>Izquierda</Button>
        </Tooltip>
      </div>
    </div>
  }
>
```tsx
import { Tooltip, Button } from "@stofli/ui";

export default function TooltipPositionsDemo() {
  return (
    <div className="grid grid-cols-2 gap-8 p-8">
      <div className="flex justify-center">
        <Tooltip position="top" content="Posición arriba">
          <Button>Arriba</Button>
        </Tooltip>
      </div>
      <div className="flex justify-center">
        <Tooltip position="right" content="Posición derecha">
          <Button>Derecha</Button>
        </Tooltip>
      </div>
      <div className="flex justify-center">
        <Tooltip position="bottom" content="Posición abajo">
          <Button>Abajo</Button>
        </Tooltip>
      </div>
      <div className="flex justify-center">
        <Tooltip position="left" content="Posición izquierda">
          <Button>Izquierda</Button>
        </Tooltip>
      </div>
    </div>
  );
}
```
</DemoTabs>

### Variantes

El tooltip soporta diferentes variantes de estilo usando la prop `variant`.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-4 justify-center">
      <Tooltip variant="default" content="Variante por defecto">
        <Button variant="default">Default</Button>
      </Tooltip>
      <Tooltip variant="primary" content="Variante primaria">
        <Button variant="primary">Primary</Button>
      </Tooltip>
      <Tooltip variant="secondary" content="Variante secundaria">
        <Button variant="secondary">Secondary</Button>
      </Tooltip>
      <Tooltip variant="success" content="Variante éxito">
        <Button variant="success">Success</Button>
      </Tooltip>
      <Tooltip variant="warning" content="Variante advertencia">
        <Button variant="warning">Warning</Button>
      </Tooltip>
      <Tooltip variant="danger" content="Variante peligro">
        <Button variant="danger">Danger</Button>
      </Tooltip>
    </div>
  }
>
```tsx
import { Tooltip, Button } from "@stofli/ui";

export default function TooltipVariantsDemo() {
  return (
    <div className="flex flex-wrap gap-4 justify-center">
      <Tooltip variant="default" content="Variante por defecto">
        <Button variant="default">Default</Button>
      </Tooltip>
      <Tooltip variant="primary" content="Variante primaria">
        <Button variant="primary">Primary</Button>
      </Tooltip>
      <Tooltip variant="secondary" content="Variante secundaria">
        <Button variant="secondary">Secondary</Button>
      </Tooltip>
      <Tooltip variant="success" content="Variante éxito">
        <Button variant="success">Success</Button>
      </Tooltip>
      <Tooltip variant="warning" content="Variante advertencia">
        <Button variant="warning">Warning</Button>
      </Tooltip>
      <Tooltip variant="danger" content="Variante peligro">
        <Button variant="danger">Danger</Button>
      </Tooltip>
    </div>
  );
}
```
</DemoTabs>

### Radios

Personaliza los bordes redondeados usando la prop `radius`.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-4 justify-center">
      <Tooltip radius="none" content="Sin bordes redondeados">
        <Button>Sin radio</Button>
      </Tooltip>
      <Tooltip radius="sm" content="Radio pequeño">
        <Button>Radio SM</Button>
      </Tooltip>
      <Tooltip radius="md" content="Radio mediano (por defecto)">
        <Button>Radio MD</Button>
      </Tooltip>
      <Tooltip radius="full" content="Radio completo">
        <Button>Radio Full</Button>
      </Tooltip>
    </div>
  }
>
```tsx
import { Tooltip, Button } from "@stofli/ui";

export default function TooltipRadiusDemo() {
  return (
    <div className="flex flex-wrap gap-4 justify-center">
      <Tooltip radius="none" content="Sin bordes redondeados">
        <Button>Sin radio</Button>
      </Tooltip>
      <Tooltip radius="sm" content="Radio pequeño">
        <Button>Radio SM</Button>
      </Tooltip>
      <Tooltip radius="md" content="Radio mediano (por defecto)">
        <Button>Radio MD</Button>
      </Tooltip>
      <Tooltip radius="full" content="Radio completo">
        <Button>Radio Full</Button>
      </Tooltip>
    </div>
  );
}
```
</DemoTabs>

### Retraso personalizado

Puedes personalizar el retraso de aparición del tooltip.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-4 justify-center">
      <Tooltip delay={100} content="Aparezco más rápido (100ms)">
        <Button>Retraso corto</Button>
      </Tooltip>
      <Tooltip delay={1000} content="Aparezco más lento (1000ms)">
        <Button>Retraso largo</Button>
      </Tooltip>
    </div>
  }
>
```tsx
import { Tooltip, Button } from "@stofli/ui";

export default function TooltipDelayDemo() {
  return (
    <div className="flex flex-wrap gap-4 justify-center">
      <Tooltip delay={100} content="Aparezco más rápido (100ms)">
        <Button>Retraso corto</Button>
      </Tooltip>
      <Tooltip delay={1000} content="Aparezco más lento (1000ms)">
        <Button>Retraso largo</Button>
      </Tooltip>
    </div>
  );
}
```
</DemoTabs>

### Contenido enriquecido

Puedes incluir contenido HTML más complejo dentro del tooltip.

<DemoTabs
  preview={
    <div className="flex justify-center">
      <Tooltip
        maxWidth="300px"
        showArrow
        content={
          <div>
            <h3 className="font-bold text-base mb-1">Título del tooltip</h3>
            <p>Puedes incluir contenido HTML complejo dentro de un tooltip, como párrafos, listas e incluso otros componentes.</p>
            <ul className="list-disc list-inside mt-2">
              <li>Primer elemento</li>
              <li>Segundo elemento</li>
            </ul>
          </div>
        }
      >
        <Button>Contenido enriquecido</Button>
      </Tooltip>
    </div>
  }
>
```tsx
import { Tooltip, Button } from "@stofli/ui";

export default function TooltipRichContentDemo() {
  return (
    <Tooltip
      maxWidth="300px"
      showArrow
      content={
        <div>
          <h3 className="font-bold text-base mb-1">Título del tooltip</h3>
          <p>Puedes incluir contenido HTML complejo dentro de un tooltip, como párrafos, listas e incluso otros componentes.</p>
          <ul className="list-disc list-inside mt-2">
            <li>Primer elemento</li>
            <li>Segundo elemento</li>
          </ul>
        </div>
      }
    >
      <Button>Contenido enriquecido</Button>
    </Tooltip>
  );
}
```
</DemoTabs>

### Con clases personalizadas

Puedes personalizar cualquier parte del componente usando las propiedades de clases personalizadas con Tailwind CSS.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-4 justify-center">
      <Tooltip
        content={
          <div className="flex items-center gap-2 p-1">
            <CheckIcon className="h-4 w-4 text-green-500" />
            <span>Tarea completada</span>
          </div>
        }
        tooltipClassName="bg-white dark:bg-zinc-900 text-zinc-800 dark:text-zinc-200 border border-zinc-100 dark:border-zinc-800 shadow-lg rounded-lg"
        position="top"
        showArrow
        arrowClassName="!bg-white dark:!bg-zinc-900 !border-zinc-100 dark:!border-zinc-800"
      >
        <Button 
          variant="success" 
          className="rounded-full h-9 w-9 flex items-center justify-center p-0"
        >
          <CheckIcon className="h-5 w-5" />
        </Button>
      </Tooltip>

      <Tooltip
        content={
          <div className="p-1">
            <div className="font-bold mb-1">Notificaciones</div>
            <div className="text-xs opacity-80">Tienes 3 mensajes sin leer</div>
          </div>
        }
        tooltipClassName="bg-gradient-to-b from-blue-700 to-blue-900 text-white border-none shadow-xl rounded-lg"
        position="bottom"
        showArrow
        arrowClassName="!bg-blue-900 !border-none"
      >
        <Button
          variant="primary"
          className="rounded-full h-9 w-9 flex items-center justify-center p-0"
        >
          <BellIcon className="h-5 w-5" />
        </Button>
      </Tooltip>

      <Tooltip
        content={
          <div className="flex items-start gap-3 p-1 w-48">
            <div className="h-8 w-8 rounded-full bg-zinc-200 dark:bg-zinc-700 flex items-center justify-center text-sm">
              <UserIcon className="h-4 w-4" />
            </div>
            <div>
              <div className="font-bold text-sm">María Rodríguez</div>
              <div className="text-xs text-zinc-600 dark:text-zinc-400">maria@example.com</div>
            </div>
            <div className="ml-auto">
              <SettingsIcon className="h-4 w-4 opacity-70" />
            </div>
          </div>
        }
        tooltipClassName="bg-white dark:bg-zinc-900 text-zinc-800 dark:text-zinc-200 border border-zinc-200 dark:border-zinc-800 shadow-2xl rounded-xl p-0"
        position="right"
        maxWidth="250px"
      >
        <Button
          variant="default"
          className="rounded-full h-9 w-9 flex items-center justify-center p-0"
        >
          <UserIcon className="h-5 w-5" />
        </Button>
      </Tooltip>
    </div>

}

>

```tsx
import { Tooltip, Button } from "@stofli/ui";
import { CheckIcon, BellIcon, UserIcon, SettingsIcon } from "lucide-react";

export default function TooltipModernDemo() {
  return (
    <div className="flex flex-wrap gap-4 justify-center">
      <Tooltip
        content={
          <div className="flex items-center gap-2 p-1">
            <CheckIcon className="h-4 w-4 text-green-500" />
            <span>Tarea completada</span>
          </div>
        }
        tooltipClassName="bg-white dark:bg-zinc-900 text-zinc-800 dark:text-zinc-200 border border-zinc-100 dark:border-zinc-800 shadow-lg rounded-lg"
        position="top"
        showArrow
        arrowClassName="!bg-white dark:!bg-zinc-900 !border-zinc-100 dark:!border-zinc-800"
      >
        <Button
          variant="success"
          className="rounded-full h-9 w-9 flex items-center justify-center p-0"
        >
          <CheckIcon className="h-5 w-5" />
        </Button>
      </Tooltip>

      <Tooltip
        content={
          <div className="p-1">
            <div className="font-bold mb-1">Notificaciones</div>
            <div className="text-xs opacity-80">Tienes 3 mensajes sin leer</div>
          </div>
        }
        tooltipClassName="bg-gradient-to-b from-blue-700 to-blue-900 text-white border-none shadow-xl rounded-lg"
        position="bottom"
        showArrow
        arrowClassName="!bg-blue-900 !border-none"
      >
        <Button
          variant="primary"
          className="rounded-full h-9 w-9 flex items-center justify-center p-0"
        >
          <BellIcon className="h-5 w-5" />
        </Button>
      </Tooltip>

      <Tooltip
        content={
          <div className="flex items-start gap-3 p-1 w-48">
            <div className="h-8 w-8 rounded-full bg-zinc-200 dark:bg-zinc-700 flex items-center justify-center text-sm">
              <UserIcon className="h-4 w-4" />
            </div>
            <div>
              <div className="font-bold text-sm">María Rodríguez</div>
              <div className="text-xs text-zinc-600 dark:text-zinc-400">
                maria@example.com
              </div>
            </div>
            <div className="ml-auto">
              <SettingsIcon className="h-4 w-4 opacity-70" />
            </div>
          </div>
        }
        tooltipClassName="bg-white dark:bg-zinc-900 text-zinc-800 dark:text-zinc-200 border border-zinc-200 dark:border-zinc-800 shadow-2xl rounded-xl p-0"
        position="right"
        maxWidth="250px"
      >
        <Button
          variant="default"
          className="rounded-full h-9 w-9 flex items-center justify-center p-0"
        >
          <UserIcon className="h-5 w-5" />
        </Button>
      </Tooltip>
    </div>
  );
}
```

</DemoTabs>

## Props

| Prop               | Tipo                                                                          | Default     | Descripción                                      |
| ------------------ | ----------------------------------------------------------------------------- | ----------- | ------------------------------------------------ |
| `children`         | `React.ReactNode`                                                             | -           | Elemento al que se adjunta el tooltip            |
| `content`          | `React.ReactNode`                                                             | -           | Contenido a mostrar en el tooltip                |
| `position`         | `"top" \| "right" \| "bottom" \| "left"`                                      | `"top"`     | Posición del tooltip respecto al elemento        |
| `variant`          | `"default" \| "primary" \| "secondary" \| "success" \| "warning" \| "danger"` | `"default"` | Variante de estilo                               |
| `delay`            | `number`                                                                      | `300`       | Retraso antes de mostrar el tooltip (ms)         |
| `maxWidth`         | `string`                                                                      | `"200px"`   | Ancho máximo del tooltip                         |
| `showArrow`        | `boolean`                                                                     | `false`     | Mostrar o no la flecha del tooltip               |
| `className`        | `string`                                                                      | `""`        | Clases adicionales para el contenedor principal  |
| `tooltipClassName` | `string`                                                                      | `""`        | Clases adicionales para el tooltip               |
| `contentClassName` | `string`                                                                      | `""`        | Clases adicionales para el contenido del tooltip |
| `arrowClassName`   | `string`                                                                      | `""`        | Clases adicionales para la flecha del tooltip    |
| `radius`           | `"none" \| "sm" \| "md" \| "full"`                                            | `"md"`      | Radio de los bordes                              |
