---
title: "Progress"
description: >-
  Un componente de barra de progreso moderno con animaciones fluidas y diferentes variantes de estilo.
---

<div className="mt-10 space-y-8">
  <div>
    <HeadingAnchor id="import">Import</HeadingAnchor>

```tsx
import { Progress } from "stofli-ui";
```


  </div>


  <div>
    <HeadingAnchor id="uso">Uso</HeadingAnchor>

Una barra de progreso simple con diferentes valores.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4">
      <Progress value={25} label="25% Completado" showValue />
      <Progress value={50} label="50% Completado" showValue />
      <Progress value={75} label="75% Completado" showValue />
      <Progress value={100} label="100% Completado" showValue />
    </div>
  }
>
```tsx
import { Progress } from "stofli-ui";

export default function ProgressDemo() {
  return (
    <div className="flex flex-col gap-4">
      <Progress value={25} label="25% Completado" showValue />
      <Progress value={50} label="50% Completado" showValue />
      <Progress value={75} label="75% Completado" showValue />
      <Progress value={100} label="100% Completado" showValue />
    </div>
  );
}
```
</DemoTabs>

    <div className="mt-6">
      <HeadingAnchor id="tamanos" level={3} >Tamaños</HeadingAnchor>

Usando la prop `size`: sm, md y lg.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4">
      <Progress size="sm" value={75} label="Pequeño" showValue variant="primary" />
      <Progress size="md" value={75} label="Mediano" showValue variant="primary" />
      <Progress size="lg" value={75} label="Grande" showValue variant="primary" />
    </div>
  }
>
```tsx
import { Progress } from "stofli-ui";

export default function ProgressSizesDemo() {
  return (
    <div className="flex flex-col gap-4">
      <Progress size="sm" value={75} label="Pequeño" showValue variant="primary" />
      <Progress size="md" value={75} label="Mediano" showValue variant="primary" />
      <Progress size="lg" value={75} label="Grande" showValue variant="primary" />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="variantes" level={3} >Variantes</HeadingAnchor>

Usando la prop `variant`: default, primary, secondary, success, warning y danger.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4">
      <Progress variant="default" value={75} label="Default" showValue />
      <Progress variant="primary" value={75} label="Primary" showValue />
      <Progress variant="secondary" value={75} label="Secondary" showValue />
      <Progress variant="success" value={75} label="Success" showValue />
      <Progress variant="warning" value={75} label="Warning" showValue />
      <Progress variant="danger" value={75} label="Danger" showValue />
    </div>
  }
>
```tsx
import { Progress } from "stofli-ui";

export default function ProgressVariantsDemo() {
  return (
    <div className="flex flex-col gap-4">
      <Progress variant="default" value={75} label="Default" showValue />
      <Progress variant="primary" value={75} label="Primary" showValue />
      <Progress variant="secondary" value={75} label="Secondary" showValue />
      <Progress variant="success" value={75} label="Success" showValue />
      <Progress variant="warning" value={75} label="Warning" showValue />
      <Progress variant="danger" value={75} label="Danger" showValue />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="animacion-rayas" level={3} >Con animación de rayas</HeadingAnchor>

Usando la prop `striped` para añadir un patrón de rayas animado.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4">
      <Progress 
        striped 
        value={75} 
        label="Con rayas animadas" 
        showValue 
        variant="primary"
      />
      <Progress 
        striped 
        value={75} 
        label="Success con rayas" 
        showValue 
        variant="success"
      />
      <Progress 
        striped 
        value={75} 
        label="Warning con rayas" 
        showValue 
        variant="warning"
      />
    </div>
  }
>
```tsx
import { Progress } from "stofli-ui";

export default function ProgressStripedDemo() {
  return (
    <div className="flex flex-col gap-4">
      <Progress 
        striped 
        value={75} 
        label="Con rayas animadas" 
        showValue 
        variant="primary"
      />
      <Progress 
        striped 
        value={75} 
        label="Success con rayas" 
        showValue 
        variant="success"
      />
      <Progress 
        striped 
        value={75} 
        label="Warning con rayas" 
        showValue 
        variant="warning"
      />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="clases-personalizadas" level={3} >Con clases personalizadas</HeadingAnchor>

Puedes personalizar cualquier parte del componente usando la propiedad `className` con clases de Tailwind CSS.

<DemoTabs
  preview={
      <Progress 
        value={75} 
        label="Barra personalizada" 
        showValue 
        variant="primary"
        className="bg-blue-100 dark:bg-blue-950/30 p-1 rounded-xl shadow-sm border border-blue-200 dark:border-blue-800"
        size="lg"
      />
  }
>
```tsx
import { Progress } from "stofli-ui";

export default function ProgressCustomClassDemo() {
  return (
      <Progress 
        value={75} 
        label="Barra personalizada" 
        showValue 
        variant="primary"
        className="bg-blue-100 dark:bg-blue-950/30 p-1 rounded-xl shadow-sm border border-blue-200 dark:border-blue-800"
        size="lg"
      />
  );
}
```
</DemoTabs>
    </div>
  </div>

  <div>
    <HeadingAnchor id="props">Props</HeadingAnchor>

| Prop                   | Tipo                                                                          | Default     | Descripción                                  |
| ---------------------- | ----------------------------------------------------------------------------- | ----------- | -------------------------------------------- |
| `value`                | `number`                                                                      | `0`         | Valor actual del progreso                    |
| `max`                  | `number`                                                                      | `100`       | Valor máximo del progreso                    |
| `size`                 | `"sm" \| "md" \| "lg"`                                                        | `"md"`      | Tamaño de la barra de progreso               |
| `variant`              | `"default" \| "primary" \| "secondary" \| "success" \| "warning" \| "danger"` | `"default"` | Variante de estilo                           |
| `showValue`            | `boolean`                                                                     | `false`     | Muestra el porcentaje actual                 |
| `animate`              | `boolean`                                                                     | `true`      | Habilita la animación de progreso            |
| `label`                | `string`                                                                      | -           | Etiqueta descriptiva                         |
| `radius`               | `"none" \| "sm" \| "md" \| "full"`                                            | `"md"`      | Radio de las esquinas                        |
| `striped`              | `boolean`                                                                     | `false`     | Añade un patrón de rayas animado             |
| `indeterminate`        | `boolean`                                                                     | `false`     | Muestra una barra de progreso indeterminada  |
| `description`          | `string`                                                                      | -           | Descripción accesible adicional              |
| `aria-label`           | `string`                                                                      | -           | Etiqueta ARIA personalizada                  |
| `aria-labelledby`      | `string`                                                                      | -           | ID del elemento que etiqueta este componente |
| `aria-describedby`     | `string`                                                                      | -           | ID del elemento que describe este componente |
| `id`                   | `string`                                                                      | -           | ID personalizado para el componente          |
| `valueText`            | `string`                                                                      | -           | Texto personalizado para el valor ARIA       |
| `className`            | `string`                                                                      | -           | Clases adicionales para el track de progreso |
| `containerClassName`   | `string`                                                                      | -           | Clases para el contenedor principal          |
| `labelClassName`       | `string`                                                                      | -           | Clases para la etiqueta de progreso          |
| `valueClassName`       | `string`                                                                      | -           | Clases para el valor de porcentaje           |
| `progressBarClassName` | `string`                                                                      | -           | Clases para la barra de progreso activa      |

  </div>
</div>

export const toc = [
  { title: "Import", href: "#import" },
  {
    title: "Uso",
    href: "#uso",
    children: [
      { title: "Tamaños", href: "#tamanos" },
      { title: "Variantes", href: "#variantes" },
      { title: "Con animación de rayas", href: "#animacion-rayas" },
      { title: "Con clases personalizadas", href: "#clases-personalizadas" },
    ],
  },
  { title: "Props", href: "#props" },
];
