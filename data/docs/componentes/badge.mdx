---
title: "Badge"
description: >-
  Un componente de insignia versátil con diferentes estilos, tamaños y características interactivas.
---

<div className="mt-10 space-y-8">
  <div>
    <HeadingAnchor id="import">Import</HeadingAnchor>

```tsx
import { Badge } from "@stofli/ui";
```

  </div>

  <div>
    <HeadingAnchor id="uso">Uso</HeadingAnchor>

El componente Badge se utiliza para mostrar etiquetas, estados o contadores.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-2">
      <Badge>Etiqueta</Badge>
    </div>
  }
>
```tsx
import { Badge } from "@stofli/ui";

export default function BadgeDemo() {
  return (
    <Badge>Etiqueta</Badge>
  );
}
```
</DemoTabs>

    <div className="mt-6">
      <HeadingAnchor id="variantes" level={3} >Variantes</HeadingAnchor>

Muestra diferentes variantes del badge.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-2">
      <Badge>Default</Badge>
      <Badge variant="primary">Primary</Badge>
      <Badge variant="secondary">Secondary</Badge>
      <Badge variant="success">Success</Badge>
      <Badge variant="warning">Warning</Badge>
      <Badge variant="danger">Danger</Badge>
      <Badge variant="outline">Outline</Badge>
    </div>
  }
>
```tsx
import { Badge } from "@stofli/ui";

export default function BadgeVariantsDemo() {
  return (
    <div className="flex flex-wrap gap-2">
      <Badge>Default</Badge>
      <Badge variant="primary">Primary</Badge>
      <Badge variant="secondary">Secondary</Badge>
      <Badge variant="success">Success</Badge>
      <Badge variant="warning">Warning</Badge>
      <Badge variant="danger">Danger</Badge>
      <Badge variant="outline">Outline</Badge>
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="tamanos" level={3} >Tamaños</HeadingAnchor>

Usando la prop `size`: sm, md y lg.

<DemoTabs
  preview={
    <div className="flex items-center gap-2">
      <Badge size="sm">Pequeño</Badge>
      <Badge size="md">Mediano</Badge>
      <Badge size="lg">Grande</Badge>
    </div>
  }
>
```tsx
import { Badge } from "@stofli/ui";

export default function BadgeSizesDemo() {
  return (
    <div className="flex items-center gap-2">
      <Badge size="sm">Pequeño</Badge>
      <Badge size="md">Mediano</Badge>
      <Badge size="lg">Grande</Badge>
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="con-punto-indicador" level={3} >Con punto indicador</HeadingAnchor>

Usando `withDot` y opcionalmente `dotColor` para personalizar el color del punto.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-2">
      <Badge withDot variant="primary">En línea</Badge>
      <Badge withDot variant="danger" dotColor="bg-red-300">Ocupado</Badge>
      <Badge withDot variant="warning">Ausente</Badge>
      <Badge withDot variant="outline">Personalizado</Badge>
    </div>
  }
>
```tsx
import { Badge } from "@stofli/ui";

export default function BadgeWithDotDemo() {
  return (
    <div className="flex flex-wrap gap-2">
      <Badge withDot variant="primary">En línea</Badge>
      <Badge withDot variant="danger" dotColor="bg-red-300">Ocupado</Badge>
      <Badge withDot variant="warning">Ausente</Badge>
      <Badge withDot variant="outline">Personalizado</Badge>
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="con-icono" level={3} >Con icono</HeadingAnchor>

Usando la prop `icon` para agregar un icono al badge.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-2">
      <Badge icon={<CheckIcon className="w-3 h-3" />} variant="success">
        Completado
      </Badge>
      <Badge icon={<AlertTriangle className="w-3 h-3" />} variant="warning">
        Pendiente
      </Badge>
      <Badge icon={<XIcon className="w-3 h-3" />} variant="danger">
        Error
      </Badge>
    </div>
  }
>
```tsx
import { Badge } from "@stofli/ui";
import { CheckIcon, AlertTriangle, XIcon } from "lucide-react";

export default function BadgeWithIconDemo() {
  return (
    <div className="flex flex-wrap gap-2">
      <Badge icon={<CheckIcon className="w-3 h-3" />} variant="success">
        Completado
      </Badge>
      <Badge icon={<AlertTriangle className="w-3 h-3" />} variant="warning">
        Pendiente
      </Badge>
      <Badge icon={<XIcon className="w-3 h-3" />} variant="danger">
        Error
      </Badge>
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="descartable" level={3} >Descartable</HeadingAnchor>

Usando `dismissible` para permitir que el badge pueda ser eliminado.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-2">
      <Badge dismissible variant="primary">Clic para eliminar</Badge>
      <Badge dismissible variant="warning">Notificación</Badge>
      <Badge dismissible variant="danger">Alerta</Badge>
    </div>
  }
>
```tsx
import { Badge } from "@stofli/ui";

export default function BadgeDismissibleDemo() {
  return (
    <div className="flex flex-wrap gap-2">
      <Badge dismissible variant="primary">Clic para eliminar</Badge>
      <Badge dismissible variant="warning">Notificación</Badge>
      <Badge dismissible variant="danger">Alerta</Badge>
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="radios" level={3} >Radios</HeadingAnchor>

Usando la prop `radius`: none, sm, md y full.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-2">
      <Badge radius="none" variant="primary">Sin radio</Badge>
      <Badge radius="sm" variant="primary">Radio pequeño</Badge>
      <Badge radius="md" variant="primary">Radio mediano</Badge>
      <Badge radius="full" variant="primary">Radio completo</Badge>
    </div>
  }
>
```tsx
import { Badge } from "@stofli/ui";

export default function BadgeRadiusDemo() {
  return (
    <div className="flex flex-wrap gap-2">
      <Badge radius="none" variant="primary">Sin radio</Badge>
      <Badge radius="sm" variant="primary">Radio pequeño</Badge>
      <Badge radius="md" variant="primary">Radio mediano</Badge>
      <Badge radius="full" variant="primary">Radio completo</Badge>
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="con-avatar" level={3} >Con Avatar</HeadingAnchor>

Combinando el Badge con el componente Avatar para notificaciones o estados de usuario.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-4">
      <div className="relative inline-flex">
        <Avatar 
          src="https://i.pravatar.cc/300?u=maria"
          alt="María García"
          size="lg"
        />
        <Badge 
          size="sm" 
          variant="success" 
          className="absolute -top-1 -right-1"
        >
          new
        </Badge>
      </div>
      <div className="relative inline-flex">
        <Avatar 
          src="https://i.pravatar.cc/300?u=juan"
          alt="Juan Pérez"
          size="lg"
        />
        <Badge 
          size="sm" 
          variant="danger" 
          className="absolute -top-1 -right-1"
        >
          2
        </Badge>
      </div>
      <div className="relative inline-flex">
        <Avatar 
          src="https://i.pravatar.cc/300?u=ana"
          alt="Ana Martínez"
          size="lg"
        />
        <Badge 
          size="sm" 
          variant="warning" 
          className="absolute bottom-0 right-0 translate-x-1/3"
        >
          5
        </Badge>
      </div>
    </div>
  }
>
```tsx
import { Badge, Avatar } from "@stofli/ui";

export default function BadgeWithAvatarDemo() {
  return (
    <div className="flex flex-wrap gap-4">
      <div className="relative inline-flex">
        <Avatar 
          src="https://i.pravatar.cc/300?u=maria"
          alt="María García"
          size="lg"
        />
        <Badge 
          size="sm" 
          variant="success" 
          className="absolute -top-1 -right-1"
        >
          new
        </Badge>
      </div>
      <div className="relative inline-flex">
        <Avatar 
          src="https://i.pravatar.cc/300?u=juan"
          alt="Juan Pérez"
          size="lg"
        />
        <Badge 
          size="sm" 
          variant="danger" 
          className="absolute -top-1 -right-1"
        >
          2
        </Badge>
      </div>
      <div className="relative inline-flex">
        <Avatar 
          src="https://i.pravatar.cc/300?u=ana"
          alt="Ana Martínez"
          size="lg"
        />
        <Badge 
          size="sm" 
          variant="warning" 
          className="absolute bottom-0 right-0 translate-x-1/3"
        >
          5
        </Badge>
      </div>
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="con-iconos" level={3} >Con iconos</HeadingAnchor>

Combinando el Badge con iconos para mostrar notificaciones o estados.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-8">
      <div className="relative inline-flex">
        <BellIcon className="w-8 h-8 text-zinc-700 dark:text-zinc-300" />
        <Badge 
          size="sm" 
          variant="danger" 
          className="absolute -top-1 -right-1.5 translate-x-1/3"
        >
          +99
        </Badge>
      </div>
      <div className="relative inline-flex">
        <MessageSquareIcon className="w-8 h-8 text-zinc-700 dark:text-zinc-300" />
        <Badge 
          size="sm" 
          variant="primary" 
          className="absolute -top-1 -right-1.5 translate-x-1/3"
        >
          3
        </Badge>
      </div>
      <div className="relative inline-flex">
        <ShoppingCartIcon className="w-8 h-8 text-zinc-700 dark:text-zinc-300" />
        <Badge 
          size="sm" 
          variant="success" 
          className="absolute -top-1 -right-1.5 translate-x-1/3"
        >
          20
        </Badge>
      </div>
    </div>
  }
>
```tsx
import { Badge } from "@stofli/ui";
import { BellIcon, MessageSquareIcon, ShoppingCartIcon } from "lucide-react";

export default function BadgeWithIconsDemo() {
  return (
    <div className="flex flex-wrap gap-8">
      <div className="relative inline-flex">
        <BellIcon className="w-8 h-8 text-zinc-700 dark:text-zinc-300" />
        <Badge 
          size="sm" 
          variant="danger" 
          className="absolute -top-1 -right-1.5 translate-x-1/3"
        >
          +99
        </Badge>
      </div>
      <div className="relative inline-flex">
        <MessageSquareIcon className="w-8 h-8 text-zinc-700 dark:text-zinc-300" />
        <Badge 
          size="sm" 
          variant="primary" 
          className="absolute -top-1 -right-1.5 translate-x-1/3"
        >
          3
        </Badge>
      </div>
      <div className="relative inline-flex">
        <ShoppingCartIcon className="w-8 h-8 text-zinc-700 dark:text-zinc-300" />
        <Badge 
          size="sm" 
          variant="success" 
          className="absolute -top-1 -right-1.5 translate-x-1/3"
        >
          20
        </Badge>
      </div>
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="clases-personalizadas" level={3} >Con clases personalizadas</HeadingAnchor>

Puedes personalizar cualquier parte del badge usando las propiedades de className con clases de Tailwind CSS.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-2">
      <Badge 
        className="bg-gradient-to-r from-indigo-500 to-purple-500 shadow-md"
        contentClassName="font-bold"
      >
        Gradiente
      </Badge>
      <Badge 
        withDot 
        dotClassName="bg-yellow-300 animate-pulse h-3 w-3"
        className="bg-zinc-800 text-white"
      >
        Animado
      </Badge>
      <Badge 
        icon={<BellIcon className="w-3 h-3" />}
        iconClassName="text-red-500 animate-bounce"
        className="bg-white border border-zinc-300 text-zinc-800 hover:bg-zinc-800 hover:text-white transition-colors"
      >
        Icono custom
      </Badge>
    </div>
  }
>
```tsx
import { Badge } from "@stofli/ui";
import { BellIcon } from "lucide-react";

export default function BadgeCustomClassesDemo() {
  return (
    <div className="flex flex-wrap gap-2">
      <Badge 
        className="bg-gradient-to-r from-indigo-500 to-purple-500 shadow-md"
        contentClassName="font-bold"
      >
        Gradiente
      </Badge>
      <Badge 
        withDot 
        dotClassName="bg-yellow-300 animate-pulse h-3 w-3"
        className="bg-zinc-800 text-white"
      >
        Animado
      </Badge>
      <Badge 
        icon={<BellIcon className="w-3 h-3" />}
        iconClassName="text-red-500 animate-bounce"
        className="bg-white border border-zinc-300 text-zinc-800 hover:bg-zinc-800 hover:text-white transition-colors"
      >
        Icono custom
      </Badge>
    </div>
  );
}
```
</DemoTabs>
    </div>
  </div>

  <div>
    <HeadingAnchor id="props">Props</HeadingAnchor>

| Prop               | Tipo                                                                                       | Default     | Descripción                                  |
| ------------------ | ------------------------------------------------------------------------------------------ | ----------- | -------------------------------------------- |
| `variant`          | `"default" \| "primary" \| "secondary" \| "success" \| "warning" \| "danger" \| "outline"` | `"default"` | Variante de estilo                           |
| `size`             | `"sm" \| "md" \| "lg"`                                                                     | `"md"`      | Tamaño del badge                             |
| `radius`           | `"none" \| "sm" \| "md" \| "full"`                                                         | `"full"`    | Radio de las esquinas                        |
| `withDot`          | `boolean`                                                                                  | `false`     | Muestra un punto indicador                   |
| `dotColor`         | `string`                                                                                   | -           | Color personalizado para el punto            |
| `icon`             | `ReactNode`                                                                                | -           | Icono a mostrar                              |
| `onClick`          | `() => void`                                                                               | -           | Función llamada al hacer clic                |
| `dismissible`      | `boolean`                                                                                  | `false`     | Permite que el badge sea eliminado           |
| `className`        | `string`                                                                                   | -           | Clases adicionales para el badge completo    |
| `dotClassName`     | `string`                                                                                   | -           | Clases adicionales para el punto indicador   |
| `iconClassName`    | `string`                                                                                   | -           | Clases adicionales para el icono             |
| `contentClassName` | `string`                                                                                   | -           | Clases adicionales para el contenido textual |
| `ariaLabel`        | `string`                                                                                   | -           | Etiqueta ARIA personalizada para el badge    |
| `dotAriaLabel`     | `string`                                                                                   | `"Indicador de estado"` | Etiqueta ARIA para el punto indicador        |
| `dismissAriaLabel` | `string`                                                                                   | `"Eliminar badge"` | Etiqueta ARIA para la acción de eliminar     |
| `id`               | `string`                                                                                   | -           | ID personalizado para el badge               |

  </div>
</div>

export const toc = [
  { title: "Import", href: "#import" },
  {
    title: "Uso",
    href: "#uso",
    children: [
      { title: "Variantes", href: "#variantes" },
      { title: "Tamaños", href: "#tamanos" },
      { title: "Con punto indicador", href: "#con-punto-indicador" },
      { title: "Con icono", href: "#con-icono" },
      { title: "Descartable", href: "#descartable" },
      { title: "Radios", href: "#radios" },
      { title: "Con Avatar", href: "#con-avatar" },
      { title: "Con iconos", href: "#con-iconos" },
      { title: "Con clases personalizadas", href: "#clases-personalizadas" },
    ],
  },
  { title: "Props", href: "#props" },
];
