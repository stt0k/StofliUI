---
title: "Switch"
description: >-
  Un componente de interruptor elegante y animado para alternar entre dos estados.
---

<div className="mt-10 space-y-8">
  <div>
    <HeadingAnchor id="import">Import</HeadingAnchor>

```tsx
import { Switch } from "@stofli/ui"
```

  </div>


  <div>
    <HeadingAnchor id="uso">Uso</HeadingAnchor>

Un switch básico con animaciones fluidas para alternar entre estados.

<DemoTabs
  preview={
    <div>
      <Switch />
    </div>
  }
>
```tsx
import { Switch } from "@stofli/ui";

export default function SwitchDemo() {
  return <Switch />;
}
```
</DemoTabs>

    <div className="mt-6">
      <HeadingAnchor id="con-etiqueta" level={3} >Con etiqueta</HeadingAnchor>

El switch puede incluir una etiqueta descriptiva a la izquierda o derecha.

<DemoTabs
  preview={
    <div className="flex">
      <div className="max-w-xs flex flex-col gap-4">
        <Switch label="Modo oscuro" />
        <Switch label="Notificaciones" labelPosition="left" />
      </div>
    </div>
  }
>
```tsx
import { Switch } from "@stofli/ui";

export default function SwitchLabelDemo() {
  return (
    <div className="flex flex-col gap-4">
      <Switch label="Modo oscuro" />
      <Switch label="Notificaciones" labelPosition="left" />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="variantes" level={3} >Variantes</HeadingAnchor>

El switch está disponible en diferentes variantes de color.

<DemoTabs
  preview={
    <div className="flex">
      <div className="max-w-xs grid grid-cols-1 gap-4">
        <Switch variant="default" label="Default" checked />
        <Switch variant="primary" label="Primary" checked />
        <Switch variant="secondary" label="Secondary" checked />
        <Switch variant="success" label="Success" checked />
        <Switch variant="warning" label="Warning" checked />
        <Switch variant="danger" label="Danger" checked />
      </div>
    </div>
  }
>
```tsx
import { Switch } from "@stofli/ui";

export default function SwitchVariantsDemo() {
  return (
    <div className="grid grid-cols-1 gap-4">
      <Switch variant="default" label="Default" checked />
      <Switch variant="primary" label="Primary" checked />
      <Switch variant="secondary" label="Secondary" checked />
      <Switch variant="success" label="Success" checked />
      <Switch variant="warning" label="Warning" checked />
      <Switch variant="danger" label="Danger" checked />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="tamanos" level={3} >Tamaños</HeadingAnchor>

El switch está disponible en tres tamaños: sm, md y lg.

<DemoTabs
  preview={
    <div className="flex">
      <div className="max-w-xs flex flex-col gap-4">
        <Switch size="sm" label="Pequeño" checked />
        <Switch size="md" label="Mediano" checked />
        <Switch size="lg" label="Grande" checked />
      </div>
    </div>
  }
>
```tsx
import { Switch } from "@stofli/ui";

export default function SwitchSizesDemo() {
  return (
    <div className="flex flex-col gap-4">
      <Switch size="sm" label="Pequeño" checked />
      <Switch size="md" label="Mediano" checked />
      <Switch size="lg" label="Grande" checked />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="con-iconos" level={3} >Con iconos</HeadingAnchor>

El switch puede mostrar iconos dentro del círculo para indicar el estado actual.

<DemoTabs
  preview={
    <div className="flex">
      <div className="max-w-xs flex flex-col gap-3">
        <Switch icon iconType="check" label="Con icono de check" variant="primary" />
        <Switch icon iconType="cross" label="Con icono de cruz" variant="danger" checked />
      </div>
    </div>
  }
>
```tsx
import { Switch } from "@stofli/ui";

export default function SwitchIconsDemo() {
  return (
    <div className="flex flex-col gap-3">
      <Switch
        icon
        iconType="check"
        label="Con icono de check"
        variant="primary"
      />
      <Switch
        icon
        iconType="cross"
        label="Con icono de cruz"
        variant="danger"
        checked
      />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="requerido" level={3} >Requerido</HeadingAnchor>

El switch puede marcarse como requerido, mostrando un asterisco junto a la etiqueta.

<DemoTabs
  preview={
    <div className="flex">
      <div className="max-w-xs">
        <Switch required label="Acepto los términos y condiciones" variant="primary" />
      </div>
    </div>
  }
>
```tsx
import { Switch } from "@stofli/ui";

export default function SwitchRequiredDemo() {
  return (
    <Switch 
      required 
      label="Acepto los términos y condiciones" 
      variant="primary" 
    />
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="deshabilitado" level={3} >Deshabilitado</HeadingAnchor>

Un switch deshabilitado que no permite interacción.

<DemoTabs
  preview={
    <div className="flex">
      <div className="max-w-xs flex flex-col gap-4">
        <Switch disabled label="Switch deshabilitado (apagado)" />
        <Switch disabled checked label="Switch deshabilitado (encendido)" variant="primary" />
      </div>
    </div>
  }
>
```tsx
import { Switch } from "@stofli/ui";

export default function SwitchDisabledDemo() {
  return (
    <div className="flex flex-col gap-4">
      <Switch disabled label="Switch deshabilitado (apagado)" />
      <Switch disabled checked label="Switch deshabilitado (encendido)" variant="primary" />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="controlado" level={3} >Controlado</HeadingAnchor>

Un ejemplo de cómo usar el switch de forma controlada.

<DemoTabs
  preview={
    <div className="flex">
      <div className="max-w-xs">
        <SwitchControlledDemo />
      </div>
    </div>
  }
>
```tsx
import { Switch } from "@stofli/ui";
import { useState } from "react";

export default function SwitchControlledDemo() {
  const [isChecked, setIsChecked] = useState(false);
  
  return (
    <div className="space-y-4">
      <Switch 
        checked={isChecked}
        onChange={setIsChecked}
        label={isChecked ? "Activado" : "Desactivado"} 
        variant="primary"
      />
      <p className="text-sm text-gray-600 dark:text-gray-400">
        El switch está actualmente: <strong>{isChecked ? "Encendido" : "Apagado"}</strong>
      </p>
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="clases-personalizadas" level={3} >Con clases personalizadas</HeadingAnchor>

Puedes personalizar cualquier parte del componente usando las propiedades de clases personalizadas con Tailwind CSS.

<DemoTabs
  preview={
    <div className="flex">
      <Switch 
        variant="primary"
        checked
        activeWrapperClassName="bg-green-500"
        inactiveWrapperClassName="bg-red-500"
      />
    </div>
  }
>
```tsx
import { Switch } from "@stofli/ui";

export default function SwitchCustomClassDemo() {
  return (
    <Switch 
      variant="primary"
      checked
      activeWrapperClassName="bg-green-500"
      inactiveWrapperClassName="bg-red-500"
    />
  );
}
```
</DemoTabs>
    </div>
  </div>

  <div>
    <HeadingAnchor id="props">Props</HeadingAnchor>

| Prop                       | Tipo                                                                          | Default     | Descripción                                                      |
| -------------------------- | ----------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------- |
| `checked`                  | `boolean`                                                                     | `false`     | Estado del switch                                                |
| `onChange`                 | `(checked: boolean) => void`                                                  | -           | Función a ejecutar cuando cambia el estado                       |
| `disabled`                 | `boolean`                                                                     | `false`     | Deshabilita el switch                                            |
| `size`                     | `"sm" \| "md" \| "lg"`                                                        | `"md"`      | Tamaño del switch                                                |
| `variant`                  | `"default" \| "primary" \| "secondary" \| "success" \| "warning" \| "danger"` | `"default"` | Variante de color                                                |
| `label`                    | `string`                                                                      | -           | Etiqueta descriptiva                                             |
| `labelPosition`            | `"left" \| "right"`                                                           | `"right"`   | Posición de la etiqueta                                          |
| `required`                 | `boolean`                                                                     | `false`     | Indica si el switch es obligatorio                               |
| `name`                     | `string`                                                                      | -           | Nombre del input para formularios                                |
| `id`                       | `string`                                                                      | -           | ID único (se genera uno automáticamente si no se proporciona)    |
| `ariaLabel`                | `string`                                                                      | -           | Etiqueta ARIA personalizada cuando no hay label visible          |
| `description`              | `string`                                                                      | -           | Descripción accesible adicional (solo visible para lectores)     |
| `descriptionId`            | `string`                                                                      | -           | ID personalizado para el elemento de descripción                 |
| `className`                | `string`                                                                      | -           | Clases adicionales para el componente principal                  |
| `circleClassName`          | `string`                                                                      | -           | Clases adicionales para el círculo del switch (ambos estados)    |
| `activeCircleClassName`    | `string`                                                                      | -           | Clases adicionales para el círculo en estado activo              |
| `inactiveCircleClassName`  | `string`                                                                      | -           | Clases adicionales para el círculo en estado inactivo            |
| `labelClassName`           | `string`                                                                      | -           | Clases adicionales para la etiqueta                              |
| `wrapperClassName`         | `string`                                                                      | -           | Clases adicionales para el contenedor del switch (ambos estados) |
| `activeWrapperClassName`   | `string`                                                                      | -           | Clases adicionales para el contenedor en estado activo           |
| `inactiveWrapperClassName` | `string`                                                                      | -           | Clases adicionales para el contenedor en estado inactivo         |
| `iconClassName`            | `string`                                                                      | -           | Clases adicionales para el icono del switch                      |
| `icon`                     | `boolean`                                                                     | `false`     | Muestra un icono dentro del círculo                              |
| `iconType`                 | `"check" \| "cross" \| "none"`                                                | `"check"`   | Tipo de icono a mostrar                                          |

  </div>
</div>

export const toc = [
  { title: "Import", href: "#import" },
  {
    title: "Uso",
    href: "#uso",
    children: [
      { title: "Con etiqueta", href: "#con-etiqueta" },
      { title: "Variantes", href: "#variantes" },
      { title: "Tamaños", href: "#tamanos" },
      { title: "Con iconos", href: "#con-iconos" },
      { title: "Requerido", href: "#requerido" },
      { title: "Deshabilitado", href: "#deshabilitado" },
      { title: "Controlado", href: "#controlado" },
      { title: "Con clases personalizadas", href: "#clases-personalizadas" },
    ],
  },
  { title: "Props", href: "#props" },
];
