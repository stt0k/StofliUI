---
title: "Calendar"
description: >-
  Un componente de calendario interactivo con soporte para selección de fechas, diferentes estilos y calendarios internacionales.
---

<div className="mt-10 space-y-8">
  <div>
    <HeadingAnchor id="instalacion">Instalación</HeadingAnchor>

```bash
# Instalar dependencias necesarias
npm install date-fns framer-motion @react-aria/i18n lucide-react

# Instalar el componente
npx @stofli/ui add calendar
```

  </div>

  <div>
    <HeadingAnchor id="uso">Uso</HeadingAnchor>

```tsx
import { Calendar } from "@stofli/ui";

export default function App() {
  return (
    <Calendar onChange={(date) => console.log("Fecha seleccionada:", date)} />
  );
}
```

  </div>

  <div>
    <HeadingAnchor id="ejemplos">Ejemplos</HeadingAnchor>

    <div className="mt-4">
      <HeadingAnchor id="basico" level={3} >Básico</HeadingAnchor>

Un calendario que muestra el mes actual y permite seleccionar fechas.

<DemoTabs
  preview={
    <div className="flex justify-center w-full">
    <Calendar />
    </div>
  }
>
```tsx
import { Calendar } from "@stofli/ui";

export default function CalendarDemo() {
  return <Calendar />;
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="fecha-minima" level={3} >Con fecha mínima</HeadingAnchor>

Usando `minValue` con la fecha actual, solo permite seleccionar fechas desde hoy en adelante.

<DemoTabs
  preview={
    <div className="flex justify-center w-full">
      <Calendar 
        variant="primary"
        minValue={new Date()} // Fecha actual como mínima
      />
    </div>
  }
>
```tsx
import { Calendar } from "@stofli/ui";

export default function CalendarMinDemo() {
  return (
    <Calendar 
      variant="primary"
      minValue={new Date()} // Solo permite seleccionar desde hoy en adelante
    />
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="fecha-maxima" level={3} >Con fecha máxima</HeadingAnchor>

Usando `maxValue` con la fecha actual, solo permite seleccionar fechas hasta hoy.

<DemoTabs
  preview={
    <div className="flex justify-center w-full">
      <Calendar 
        variant="primary"
        maxValue={new Date()} // Fecha actual como máxima
      />
    </div>
  }
>
```tsx
import { Calendar } from "@stofli/ui";

export default function CalendarMaxDemo() {
  return (
    <Calendar 
      variant="primary"
      maxValue={new Date()} // Solo permite seleccionar hasta hoy
    />
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="variantes" level={3} >Variantes</HeadingAnchor>

Seis estilos diferentes usando la prop `variant`: default, primary, secondary, success, warning y danger.

<DemoTabs
  preview={
    <div className="flex flex-wrap justify-center gap-4 w-full">
      <Calendar variant="default" />
      <Calendar variant="primary" />
      <Calendar variant="secondary" />
      <Calendar variant="success" />
      <Calendar variant="warning" />
      <Calendar variant="danger" />
    </div>
  }
>
```tsx
import { Calendar } from "@stofli/ui";

export default function CalendarVariantsDemo() {
  return (
    <div className="flex flex-wrap justify-between gap-4">
      <Calendar variant="default" />
      <Calendar variant="primary" />
      <Calendar variant="secondary" />
      <Calendar variant="success" />
      <Calendar variant="warning" />
      <Calendar variant="danger" />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="calendarios-internacionales" level={3} >Calendarios Internacionales</HeadingAnchor>

El componente soporta diferentes sistemas de calendario y localizaciones usando las props `calendar` y `locale`. La prop `calendar` permite especificar el sistema de calendario (por ejemplo: "chinese", "islamic-civil", "buddhist", "hebrew", etc.) y `locale` define el idioma y la región (por ejemplo: "zh-CN" para chino, "ar-SA" para árabe, "th-TH" para tailandés).

<DemoTabs
  preview={
    <div className="flex flex-wrap justify-center gap-4 w-full">
      <Calendar calendar="chinese" locale="zh-CN" variant="primary" />
      <Calendar calendar="islamic-civil" locale="ar-SA" variant="primary" />
      <Calendar calendar="buddhist" locale="th-TH" variant="primary" />
      <Calendar calendar="hebrew" locale="he-IL" variant="primary" />
    </div>
  }
>
```tsx
import { Calendar } from "@stofli/ui";

export default function CalendarSystemsDemo() {
  return (
    <div className="flex flex-wrap justify-between gap-4">
      <Calendar calendar="chinese" locale="zh-CN" variant="primary" />
      <Calendar calendar="islamic-civil" locale="ar-SA" variant="primary" />
      <Calendar calendar="buddhist" locale="th-TH" variant="primary" />
      <Calendar calendar="hebrew" locale="he-IL" variant="primary" />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="radios" level={3} >Radios</HeadingAnchor>

Cuatro opciones de redondeo usando la prop `radius`: none, sm, md y full.

<DemoTabs
  preview={
    <div className="flex flex-wrap justify-center gap-4 w-full">
      <Calendar radius="none" variant="primary" />
      <Calendar radius="sm" variant="primary" />
      <Calendar radius="md" variant="primary" />
      <Calendar radius="full" variant="primary" />
    </div>
  }
>
```tsx
import { Calendar } from "@stofli/ui";

export default function CalendarRadiusDemo() {
  return (
    <div className="flex flex-wrap justify-between gap-4">
      <Calendar radius="none" variant="primary" />
      <Calendar radius="sm" variant="primary" />
      <Calendar radius="md" variant="primary" />
      <Calendar radius="full" variant="primary" />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="solo-lectura" level={3} >Solo Lectura</HeadingAnchor>

Usando `readOnly` y `value`, muestra un calendario no interactivo fijado en marzo de 2025.

<DemoTabs
  preview={
    <div className="flex justify-center w-full">
      <Calendar 
        readOnly 
        variant="primary"
        value={new Date(2025, 2, 15)} // 15 de marzo de 2025
      />
    </div>
  }
>
```tsx
import { Calendar } from "@stofli/ui";

export default function CalendarReadOnlyDemo() {
  return (
    <Calendar 
      readOnly 
      variant="primary"
      value={new Date(2025, 2, 15)} // 15 de marzo de 2025
    />
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="clases-personalizadas" level={3} >Con clases personalizadas</HeadingAnchor>

Personaliza cualquier parte del calendario usando las propiedades de className con clases de Tailwind CSS.

<DemoTabs
  preview={
    <div className="flex justify-center w-full">
      <Calendar 
        className="border-2 border-purple-300 dark:border-purple-700 rounded-xl shadow-lg shadow-purple-200 dark:shadow-purple-900/30"
        headerClassName="mb-6"
        weekdaysClassName="gap-2 mb-4"
        weekdayClassName="font-bold text-purple-600 dark:text-purple-400"
        daysContainerClassName="gap-2"
        dayClassName="rounded-full [&:not([data-selected=true])]:hover:bg-purple-400 [&:not([data-selected=true])]:hover:text-white [&:not([data-selected=true])]:dark:hover:bg-purple-500 transition-colors duration-150"
        selectedDayClassName="bg-purple-500 dark:bg-purple-400 text-white ring-4 ring-purple-200 dark:ring-purple-800"
        navigationButtonClassName="text-purple-600 dark:text-purple-400 hover:bg-purple-100 dark:hover:bg-purple-800/50"
        monthTitleClassName="text-2xl text-purple-800 dark:text-purple-300"
        variant="secondary"
      />
    </div>
  }
>
```tsx
import { Calendar } from "@stofli/ui";

export default function CalendarCustomClassesDemo() {
  return (
    <Calendar 
      className="border-2 border-purple-300 dark:border-purple-700 rounded-xl shadow-lg shadow-purple-200 dark:shadow-purple-900/30"
      headerClassName="mb-6"
      weekdaysClassName="gap-2 mb-4"
      weekdayClassName="font-bold text-purple-600 dark:text-purple-400"
      daysContainerClassName="gap-2"
      dayClassName="rounded-full [&:not([data-selected=true])]:hover:bg-purple-400 [&:not([data-selected=true])]:hover:text-white [&:not([data-selected=true])]:dark:hover:bg-purple-500 transition-colors duration-150"
      selectedDayClassName="bg-purple-500 dark:bg-purple-400 text-white ring-4 ring-purple-200 dark:ring-purple-800"
      navigationButtonClassName="text-purple-600 dark:text-purple-400 hover:bg-purple-100 dark:hover:bg-purple-800/50"
      monthTitleClassName="text-2xl text-purple-800 dark:text-purple-300"
      variant="secondary"
    />
  );
}
```
</DemoTabs>
    </div>
  </div>

  <div>
    <HeadingAnchor id="props">Props</HeadingAnchor>

| Prop                        | Tipo                                                                                                                                                                                                 | Default     | Descripción                                    |
| --------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ---------------------------------------------- |
| `value`                     | `Date`                                                                                                                                                                                               | -           | Fecha seleccionada                             |
| `onChange`                  | `(date: Date) => void`                                                                                                                                                                               | -           | Función llamada al seleccionar una fecha       |
| `className`                 | `string`                                                                                                                                                                                             | -           | Clases para el contenedor principal            |
| `headerClassName`           | `string`                                                                                                                                                                                             | -           | Clases para la cabecera del calendario         |
| `weekdaysClassName`         | `string`                                                                                                                                                                                             | -           | Clases para el contenedor de días de la semana |
| `weekdayClassName`          | `string`                                                                                                                                                                                             | -           | Clases para cada día de la semana              |
| `daysContainerClassName`    | `string`                                                                                                                                                                                             | -           | Clases para el contenedor de días              |
| `dayClassName`              | `string`                                                                                                                                                                                             | -           | Clases para cada día del calendario            |
| `selectedDayClassName`      | `string`                                                                                                                                                                                             | -           | Clases para el día seleccionado                |
| `disabledDayClassName`      | `string`                                                                                                                                                                                             | -           | Clases para los días deshabilitados            |
| `calendarTitleClassName`    | `string`                                                                                                                                                                                             | -           | Clases para el título del calendario           |
| `monthTitleClassName`       | `string`                                                                                                                                                                                             | -           | Clases para el título del mes                  |
| `navigationButtonClassName` | `string`                                                                                                                                                                                             | -           | Clases para los botones de navegación          |
| `variant`                   | `"default" \| "primary" \| "secondary" \| "success" \| "warning" \| "danger"`                                                                                                                        | `"default"` | Variante de estilo                             |
| `radius`                    | `"none" \| "sm" \| "md" \| "full"`                                                                                                                                                                   | `"md"`      | Radio de las esquinas                          |
| `readOnly`                  | `boolean`                                                                                                                                                                                            | `false`     | Modo solo lectura                              |
| `minValue`                  | `Date`                                                                                                                                                                                               | -           | Fecha mínima seleccionable                     |
| `maxValue`                  | `Date`                                                                                                                                                                                               | -           | Fecha máxima seleccionable                     |
| `calendar`                  | `"buddhist" \| "ethiopic" \| "ethioaa" \| "coptic" \| "hebrew" \| "indian" \| "islamic-civil" \| "islamic-tbla" \| "islamic-umalqura" \| "japanese" \| "persian" \| "roc" \| "gregory" \| "chinese"` | `"gregory"` | Sistema de calendario                          |
| `locale`                    | `string`                                                                                                                                                                                             | `"es"`      | Código de idioma (ej: "es", "en", "zh-CN")     |

  </div>
</div>

export const toc = [
  { title: "Instalación", href: "#instalacion" },
  { title: "Uso", href: "#uso" },
  {
    title: "Ejemplos",
    href: "#ejemplos",
    children: [
      { title: "Básico", href: "#basico" },
      { title: "Con fecha mínima", href: "#fecha-minima" },
      { title: "Con fecha máxima", href: "#fecha-maxima" },
      { title: "Variantes", href: "#variantes" },
      {
        title: "Calendarios Internacionales",
        href: "#calendarios-internacionales",
      },
      { title: "Radios", href: "#radios" },
      { title: "Solo Lectura", href: "#solo-lectura" },
      { title: "Con clases personalizadas", href: "#clases-personalizadas" },
    ],
  },
  { title: "Props", href: "#props" },
];
