---
title: "Spinner"
description: >-
  Un componente de spinner para indicar estados de carga con diferentes estilos y animaciones.
---

<div className="mt-10 space-y-8">
  <div>
    <HeadingAnchor id="instalacion">Instalación</HeadingAnchor>

```bash
# Instalar el componente
npx @stofli/ui add spinner
```

  </div>

  <div>
    <HeadingAnchor id="uso">Uso</HeadingAnchor>

```tsx
import { Spinner } from "@stofli/ui";

export default function App() {
  return <Spinner />;
}
```

  </div>

  <div>
    <HeadingAnchor id="ejemplos">Ejemplos</HeadingAnchor>

    <div className="mt-4">
      <HeadingAnchor id="basico" level={3} >Básico</HeadingAnchor>

El spinner básico que se muestra como un indicador de carga.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-8 items-center justify-start">
      <Spinner />
    </div>
  }
>
```tsx
import { Spinner } from "@stofli/ui";

export default function SpinnerDemo() {
  return <Spinner />;
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="tamanos" level={3} >Tamaños</HeadingAnchor>

El spinner está disponible en varios tamaños: xs, sm, md, lg y xl.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-8 items-center justify-start">
      <Spinner size="xs" />
      <Spinner size="sm" />
      <Spinner size="md" />
      <Spinner size="lg" />
      <Spinner size="xl" />
    </div>
  }
>
```tsx
import { Spinner } from "@stofli/ui";

export default function SpinnerSizesDemo() {
  return (
    <div className="flex flex-wrap gap-8 items-center justify-start">
      <Spinner size="xs" />
      <Spinner size="sm" />
      <Spinner size="md" />
      <Spinner size="lg" />
      <Spinner size="xl" />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="variantes" level={3} >Variantes</HeadingAnchor>

El spinner está disponible en diferentes variantes de color.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-8 items-center justify-start">
      <Spinner variant="default" />
      <Spinner variant="primary" />
      <Spinner variant="secondary" />
      <Spinner variant="success" />
      <Spinner variant="warning" />
      <Spinner variant="danger" />
    </div>
  }
>
```tsx
import { Spinner } from "@stofli/ui";

export default function SpinnerVariantsDemo() {
  return (
    <div className="flex flex-wrap gap-8 items-center justify-start">
      <Spinner variant="default" />
      <Spinner variant="primary" />
      <Spinner variant="secondary" />
      <Spinner variant="success" />
      <Spinner variant="warning" />
      <Spinner variant="danger" />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="tipos" level={3} >Tipos</HeadingAnchor>

El spinner está disponible en cinco tipos diferentes: border, dots, grow, wave y lines.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-8 items-center justify-start">
      <Spinner type="border" variant="primary" />
      <Spinner type="dots" variant="primary" />
      <Spinner type="grow" variant="primary" />
      <Spinner type="wave" variant="primary" />
      <Spinner type="lines" variant="primary" />
    </div>
  }
>
```tsx
import { Spinner } from "@stofli/ui";

export default function SpinnerTypesDemo() {
  return (
    <div className="flex flex-wrap gap-8 items-center justify-start">
      <Spinner type="border" variant="primary" />
      <Spinner type="dots" variant="primary" />
      <Spinner type="grow" variant="primary" />
      <Spinner type="wave" variant="primary" />
      <Spinner type="lines" variant="primary" />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="velocidades" level={3} >Velocidades</HeadingAnchor>

El spinner se puede configurar con diferentes velocidades de animación.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-8 items-center justify-start">
      <Spinner speed="slow" variant="primary" />
      <Spinner speed="normal" variant="primary" />
      <Spinner speed="fast" variant="primary" />
    </div>
  }
>
```tsx
import { Spinner } from "@stofli/ui";

export default function SpinnerSpeedsDemo() {
  return (
    <div className="flex flex-wrap gap-8 items-center justify-start">
      <Spinner speed="slow" variant="primary" />
      <Spinner speed="normal" variant="primary" />
      <Spinner speed="fast" variant="primary" />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="con-etiqueta" level={3} >Con etiqueta</HeadingAnchor>

El spinner puede incluir una etiqueta de texto en diferentes posiciones.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-8 items-center justify-start">
      <Spinner label="Cargando..." labelPosition="right" variant="primary" />
      <Spinner label="Cargando..." labelPosition="left" variant="primary" />
      <Spinner label="Cargando..." labelPosition="top" variant="primary" />
      <Spinner label="Cargando..." labelPosition="bottom" variant="primary" />
    </div>
  }
>
```tsx
import { Spinner } from "@stofli/ui";

export default function SpinnerWithLabelDemo() {
  return (
    <div className="flex flex-wrap gap-8 items-center justify-start">
      <Spinner label="Cargando..." labelPosition="right" variant="primary" />
      <Spinner label="Cargando..." labelPosition="left" variant="primary" />
      <Spinner label="Cargando..." labelPosition="top" variant="primary" />
      <Spinner label="Cargando..." labelPosition="bottom" variant="primary" />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="combinaciones" level={3} >Combinaciones</HeadingAnchor>

Puedes combinar diferentes propiedades para personalizar el spinner según tus necesidades.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-8 items-center justify-start">
      <Spinner type="border" size="lg" variant="primary" label="Guardando..." labelPosition="right" />
      <Spinner type="dots" variant="success" label="Procesando..." labelPosition="bottom" />
      <Spinner type="grow" variant="danger" size="lg" speed="slow" />
    </div>
  }
>
```tsx
import { Spinner } from "@stofli/ui";

export default function SpinnerCombinationsDemo() {
  return (
    <div className="flex flex-wrap gap-8 items-center justify-start">
      <Spinner type="border" size="lg" variant="primary" label="Guardando..." labelPosition="right" />
      <Spinner type="dots" variant="success" label="Procesando..." labelPosition="bottom" />
      <Spinner type="grow" variant="danger" size="lg" speed="slow" />
    </div>
  );
}
```
</DemoTabs>
    </div>

    <div className="mt-6">
      <HeadingAnchor id="clases-personalizadas" level={3} >Con clases personalizadas</HeadingAnchor>

Puedes personalizar cualquier parte del componente usando la propiedad className con clases de Tailwind CSS.

<DemoTabs
  preview={
      <Spinner 
        type="border" 
        variant="success"
        className="p-4 bg-green-50 dark:bg-green-950/30 rounded-lg shadow-md border border-green-200 dark:border-green-800"
        spinnerClassName="opacity-90"
        label="Cargando..." 
        labelClassName="font-semibold text-green-800 dark:text-green-300"
      />
  }
>
```tsx
import { Spinner } from "@stofli/ui";

export default function SpinnerCustomClassDemo() {
  return (
    <Spinner 
        type="border" 
        variant="success"
        className="p-4 bg-green-50 dark:bg-green-950/30 rounded-lg shadow-md border border-green-200 dark:border-green-800"
        spinnerClassName="opacity-90"
        label="Cargando..." 
        labelClassName="font-semibold text-green-800 dark:text-green-300"
      />
  );
}
```
</DemoTabs>
    </div>
  </div>

  <div>
    <HeadingAnchor id="props">Props</HeadingAnchor>

| Prop                 | Tipo                                                                          | Default     | Descripción                            |
| -------------------- | ----------------------------------------------------------------------------- | ----------- | -------------------------------------- |
| `size`               | `"xs" \| "sm" \| "md" \| "lg" \| "xl"`                                        | `"md"`      | Tamaño del spinner                     |
| `variant`            | `"default" \| "primary" \| "secondary" \| "success" \| "warning" \| "danger"` | `"default"` | Variante de color del spinner          |
| `speed`              | `"slow" \| "normal" \| "fast"`                                                | `"normal"`  | Velocidad de la animación              |
| `type`               | `"border" \| "dots" \| "grow" \| "wave" \| "lines"`                           | `"border"`  | Tipo de animación del spinner          |
| `label`              | `string`                                                                      | -           | Texto descriptivo para el spinner      |
| `labelPosition`      | `"left" \| "right" \| "top" \| "bottom"`                                      | `"right"`   | Posición del texto respecto al spinner |
| `className`          | `string`                                                                      | -           | Clases adicionales para el componente  |
| `containerClassName` | `string`                                                                      | -           | Clases para el contenedor principal    |
| `spinnerClassName`   | `string`                                                                      | -           | Clases para el elemento spinner        |
| `labelClassName`     | `string`                                                                      | -           | Clases para el texto de la etiqueta    |

  </div>
</div>

export const toc = [
  { title: "Instalación", href: "#instalacion" },
  { title: "Uso", href: "#uso" },
  {
    title: "Ejemplos",
    href: "#ejemplos",
    children: [
      { title: "Básico", href: "#basico" },
      { title: "Tamaños", href: "#tamanos" },
      { title: "Variantes", href: "#variantes" },
      { title: "Tipos", href: "#tipos" },
      { title: "Velocidades", href: "#velocidades" },
      { title: "Con etiqueta", href: "#con-etiqueta" },
      { title: "Combinaciones", href: "#combinaciones" },
      { title: "Con clases personalizadas", href: "#clases-personalizadas" },
    ],
  },
  { title: "Props", href: "#props" },
];
