---
title: "Button"
description: >-
  Un componente de botón con varias variantes y tamaños, incluyendo estados de carga y estilos diferentes.
---

## Instalación

```bash
# Instalar dependencias necesarias
npm install framer-motion lucide-react

# Instalar el componente
npx @stofli/ui add button
```

## Uso

```tsx
import { Button } from "@stofli/ui";

export default function App() {
  return (
    <Button variant="primary" size="md">
      Botón primario
    </Button>
  );
}
```

## Ejemplos

### Variantes

El botón tiene varias variantes que puedes usar para diferentes contextos.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-2">
      <Button variant="default">Default</Button>
      <Button variant="primary">Primary</Button>
      <Button variant="secondary">Secondary</Button>
      <Button variant="success">Success</Button>
      <Button variant="warning">Warning</Button>
      <Button variant="danger">Danger</Button>
      <Button variant="outline">Outline</Button>
      <Button variant="ghost">Ghost</Button>
      <Button variant="link">Link</Button>
    </div>
  }
>
```tsx
import { Button } from "@stofli/ui";

export default function ButtonVariantsDemo() {
  return (
    <div className="flex flex-wrap gap-2">
      <Button variant="default">Default</Button>
      <Button variant="primary">Primary</Button>
      <Button variant="secondary">Secondary</Button>
      <Button variant="success">Success</Button>
      <Button variant="warning">Warning</Button>
      <Button variant="danger">Danger</Button>
      <Button variant="outline">Outline</Button>
      <Button variant="ghost">Ghost</Button>
      <Button variant="link">Link</Button>
    </div>
  );
}
```
</DemoTabs>

### Tamaños

Personaliza el tamaño del botón según tus necesidades.

<DemoTabs
  preview={
    <div className="flex flex-wrap items-center gap-2">
      <Button size="xs">Extra Small</Button>
      <Button size="sm">Small</Button>
      <Button size="md">Medium</Button>
      <Button size="lg">Large</Button>
      <Button size="xl">Extra Large</Button>
      <Button size="icon"><StarIcon className="h-4 w-4" /></Button>
    </div>
  }
>
```tsx
import { Button } from "@stofli/ui";
import { StarIcon } from "lucide-react";

export default function ButtonSizesDemo() {
  return (
    <div className="flex flex-wrap items-center gap-2">
      <Button size="xs">Extra Small</Button>
      <Button size="sm">Small</Button>
      <Button size="md">Medium</Button>
      <Button size="lg">Large</Button>
      <Button size="xl">Extra Large</Button>
      <Button size="icon"><StarIcon className="h-4 w-4" /></Button>
    </div>
  );
}
```
</DemoTabs>

### Radios

Personaliza el radio de las esquinas del botón.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-2">
      <Button variant="primary" radius="none">Sin radio</Button>
      <Button variant="primary" radius="sm">Radio pequeño</Button>
      <Button variant="primary" radius="md">Radio mediano</Button>
      <Button variant="primary" radius="lg">Radio grande</Button>
      <Button variant="primary" radius="full">Radio completo</Button>
    </div>
  }
>
```tsx
import { Button } from "@stofli/ui";

export default function ButtonRadiusDemo() {
  return (
    <div className="flex flex-wrap gap-2">
      <Button variant="primary" radius="none">Sin radio</Button>
      <Button variant="primary" radius="sm">Radio pequeño</Button>
      <Button variant="primary" radius="md">Radio mediano</Button>
      <Button variant="primary" radius="lg">Radio grande</Button>
      <Button variant="primary" radius="full">Radio completo</Button>
    </div>
  );
}
```
</DemoTabs>

### Efecto Ripple

Los botones tienen un efecto de onda expansiva (ripple) que se activa al hacer clic.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-2">
      <Button variant="primary">Con efecto ripple</Button>
      <Button variant="secondary" withRipple={false}>Sin efecto ripple</Button>
      <Button variant="outline" rippleColor="#3498DB">Ripple personalizado</Button>
    </div>
  }
>
```tsx
import { Button } from "@stofli/ui";

export default function ButtonRippleDemo() {
  return (
    <div className="flex flex-wrap gap-2">
      <Button variant="primary">Con efecto ripple</Button>
      <Button variant="secondary" withRipple={false}>Sin efecto ripple</Button>
      <Button variant="outline" rippleColor="#3498DB">Ripple personalizado</Button>
    </div>
  );
}
```
</DemoTabs>

### Efecto de Clic

Los botones tienen un sutil efecto de escala al hacer clic, creando una sensación táctil de presión.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-2">
      <Button variant="primary">Haz clic para ver el efecto</Button>
      <Button variant="secondary">Pruébalo</Button>
    </div>
  }
>
```tsx
import { Button } from "@stofli/ui";

export default function ButtonClickEffectDemo() {
  return (
    <div className="flex flex-wrap gap-2">
      <Button variant="primary">Haz clic para ver el efecto</Button>
      <Button variant="secondary">Pruébalo</Button>
    </div>
  );
}
```
</DemoTabs>

### Con iconos

Añade iconos a los botones para mejorar la experiencia visual.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-2">
      <Button variant="primary" leftIcon={<MessageSquareIcon className="h-4 w-4" />}>
        Mensaje
      </Button>
      <Button variant="success" rightIcon={<ShoppingCartIcon className="h-4 w-4" />}>
        Añadir al carrito
      </Button>
      <Button variant="secondary" withArrow>
        Continuar
      </Button>
    </div>
  }
>
```tsx
import { Button } from "@stofli/ui";
import { MessageSquareIcon, ShoppingCartIcon } from "lucide-react";

export default function ButtonWithIconsDemo() {
  return (
    <div className="flex flex-wrap gap-2">
      <Button variant="primary" leftIcon={<MessageSquareIcon className="h-4 w-4" />}>
        Mensaje
      </Button>
      <Button variant="success" rightIcon={<ShoppingCartIcon className="h-4 w-4" />}>
        Añadir al carrito
      </Button>
      <Button variant="secondary" withArrow>
        Continuar
      </Button>
    </div>
  );
}
```
</DemoTabs>

### Estados de Carga

Muestra un estado de carga en el botón.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-2">
      <Button variant="primary" isLoading>
        Cargando
      </Button>
      <Button variant="outline" isLoading>
        Guardando
      </Button>
    </div>
  }
>
```tsx
import { Button } from "@stofli/ui";

export default function ButtonLoadingDemo() {
  return (
    <div className="flex flex-wrap gap-2">
      <Button variant="primary" isLoading>
        Cargando
      </Button>
      <Button variant="outline" isLoading>
        Guardando
      </Button>
    </div>
  );
}
```
</DemoTabs>

### Ancho completo

Botones que ocupan todo el ancho disponible.

<DemoTabs
  preview={
    <div className="space-y-2 w-full max-w-xs">
      <Button variant="primary" fullWidth>
        Botón primario
      </Button>
      <Button variant="outline" fullWidth>
        Botón outline
      </Button>
    </div>
  }
>
```tsx
import { Button } from "@stofli/ui";

export default function ButtonFullWidthDemo() {
  return (
    <div className="space-y-2 w-full max-w-xs">
      <Button variant="primary" fullWidth>
        Botón primario
      </Button>
      <Button variant="outline" fullWidth>
        Botón outline
      </Button>
    </div>
  );
}
```
</DemoTabs>

### Deshabilitado

Muestra un estado deshabilitado para los botones.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-2">
      <Button variant="primary" disabled>
        Botón deshabilitado
      </Button>
      <Button variant="outline" disabled>
        No disponible
      </Button>
    </div>
  }
>
```tsx
import { Button } from "@stofli/ui";

export default function ButtonDisabledDemo() {
  return (
    <div className="flex flex-wrap gap-2">
      <Button variant="primary" disabled>
        Botón deshabilitado
      </Button>
      <Button variant="outline" disabled>
        No disponible
      </Button>
    </div>
  );
}
```
</DemoTabs>

### Con clases personalizadas

Personaliza cualquier parte del botón usando las propiedades de className con clases de Tailwind CSS.

<DemoTabs
  preview={
    <div className="flex">
      <Button 
        variant="default"
        className="rounded-full bg-black text-white dark:bg-white dark:text-black hover:bg-black/80 dark:hover:bg-white/80  px-6 group"
        withArrow
        arrowClassName="text-white dark:text-black transition-transform duration-300 group-hover:translate-x-1"
        rippleClassName="bg-blue-400/30 dark:bg-purple-600/60"
      >
        Continuar
      </Button>
    </div>
  }
>
```tsx
import { Button } from "@stofli/ui";

export default function ButtonCustomClassesDemo() {
  return (
    <div className="flex">
      <Button 
        variant="default"
        className="rounded-full bg-black text-white dark:bg-white dark:text-black hover:bg-black/80 dark:hover:bg-white/80  px-6 group"
        withArrow
        arrowClassName="text-white dark:text-black transition-transform duration-300 group-hover:translate-x-1"
        rippleClassName="bg-blue-400/30 dark:bg-purple-600/60"
      >
        Continuar
      </Button>
    </div>
  );
}
```
</DemoTabs>

## Props

| Prop                 | Tipo                                                                                                            | Default     | Descripción                                       |
| -------------------- | --------------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------- |
| `variant`            | `"default" \| "primary" \| "secondary" \| "success" \| "warning" \| "danger" \| "outline" \| "ghost" \| "link"` | `"default"` | Variante visual del botón                         |
| `size`               | `"xs" \| "sm" \| "md" \| "lg" \| "xl" \| "icon"`                                                                | `"md"`      | Tamaño del botón                                  |
| `radius`             | `"none" \| "sm" \| "md" \| "lg" \| "full"`                                                                      | `"md"`      | Radio de las esquinas del botón                   |
| `fullWidth`          | `boolean`                                                                                                       | `false`     | Si el botón ocupa todo el ancho disponible        |
| `withRing`           | `boolean`                                                                                                       | `false`     | Si el botón tiene un anillo decorativo            |
| `withRipple`         | `boolean`                                                                                                       | `true`      | Activa el efecto de onda expansiva al hacer clic  |
| `rippleColor`        | `string`                                                                                                        | -           | Color personalizado para el efecto ripple         |
| `isLoading`          | `boolean`                                                                                                       | `false`     | Muestra un indicador de carga                     |
| `leftIcon`           | `ReactNode`                                                                                                     | -           | Icono a mostrar a la izquierda del texto          |
| `rightIcon`          | `ReactNode`                                                                                                     | -           | Icono a mostrar a la derecha del texto            |
| `withArrow`          | `boolean`                                                                                                       | `false`     | Muestra una flecha animada a la derecha del texto |
| `disabled`           | `boolean`                                                                                                       | `false`     | Deshabilita el botón                              |
| `children`           | `ReactNode`                                                                                                     | -           | Contenido del botón                               |
| `className`          | `string`                                                                                                        | -           | Clases adicionales para personalizar el botón     |
| `rippleClassName`    | `string`                                                                                                        | -           | Clases para el efecto ripple                      |
| `loaderClassName`    | `string`                                                                                                        | -           | Clases para el indicador de carga                 |
| `leftIconClassName`  | `string`                                                                                                        | -           | Clases para el icono izquierdo                    |
| `rightIconClassName` | `string`                                                                                                        | -           | Clases para el icono derecho                      |
| `contentClassName`   | `string`                                                                                                        | -           | Clases para el contenido del botón                |
| `arrowClassName`     | `string`                                                                                                        | -           | Clases para la flecha                             |
