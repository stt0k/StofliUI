---
title: "Spinner"
description: "Un componente de spinner para indicar estados de carga con diferentes estilos y animaciones."
---

## Instalación

```bash
# Instalar el componente
npx @stofli/ui add spinner
```

## Uso

```tsx
import { Spinner } from "@stofli/ui";

export default function App() {
  return <Spinner />;
}
```

## Ejemplos

### Básico

El spinner básico que se muestra como un indicador de carga.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-8 items-center justify-start">
      <Spinner />
    </div>
  }
>
```tsx
import { Spinner } from "@stofli/ui";

export default function SpinnerDemo() {
  return <Spinner />;
}
```
</DemoTabs>

### Tamaños

El spinner está disponible en varios tamaños: xs, sm, md, lg y xl.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-8 items-center justify-start">
      <Spinner size="xs" />
      <Spinner size="sm" />
      <Spinner size="md" />
      <Spinner size="lg" />
      <Spinner size="xl" />
    </div>
  }
>
```tsx
import { Spinner } from "@stofli/ui";

export default function SpinnerSizesDemo() {
  return (
    <div className="flex flex-wrap gap-8 items-center justify-start">
      <Spinner size="xs" />
      <Spinner size="sm" />
      <Spinner size="md" />
      <Spinner size="lg" />
      <Spinner size="xl" />
    </div>
  );
}
```
</DemoTabs>

### Variantes

El spinner está disponible en diferentes variantes de color.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-8 items-center justify-start">
      <Spinner variant="default" />
      <Spinner variant="primary" />
      <Spinner variant="secondary" />
      <Spinner variant="success" />
      <Spinner variant="warning" />
      <Spinner variant="danger" />
    </div>
  }
>
```tsx
import { Spinner } from "@stofli/ui";

export default function SpinnerVariantsDemo() {
  return (
    <div className="flex flex-wrap gap-8 items-center justify-start">
      <Spinner variant="default" />
      <Spinner variant="primary" />
      <Spinner variant="secondary" />
      <Spinner variant="success" />
      <Spinner variant="warning" />
      <Spinner variant="danger" />
    </div>
  );
}
```
</DemoTabs>

### Tipos

El spinner está disponible en cinco tipos diferentes: border, dots, grow, wave y lines.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-8 items-center justify-start">
      <Spinner type="border" variant="primary" />
      <Spinner type="dots" variant="primary" />
      <Spinner type="grow" variant="primary" />
      <Spinner type="wave" variant="primary" />
      <Spinner type="lines" variant="primary" />
    </div>
  }
>
```tsx
import { Spinner } from "@stofli/ui";

export default function SpinnerTypesDemo() {
  return (
    <div className="flex flex-wrap gap-8 items-center justify-start">
      <Spinner type="border" variant="primary" />
      <Spinner type="dots" variant="primary" />
      <Spinner type="grow" variant="primary" />
      <Spinner type="wave" variant="primary" />
      <Spinner type="lines" variant="primary" />
    </div>
  );
}
```
</DemoTabs>

### Velocidades

El spinner se puede configurar con diferentes velocidades de animación.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-8 items-center justify-start">
      <Spinner speed="slow" variant="primary" />
      <Spinner speed="normal" variant="primary" />
      <Spinner speed="fast" variant="primary" />
    </div>
  }
>
```tsx
import { Spinner } from "@stofli/ui";

export default function SpinnerSpeedsDemo() {
  return (
    <div className="flex flex-wrap gap-8 items-center justify-start">
      <Spinner speed="slow" variant="primary" />
      <Spinner speed="normal" variant="primary" />
      <Spinner speed="fast" variant="primary" />
    </div>
  );
}
```
</DemoTabs>

### Con etiqueta

El spinner puede incluir una etiqueta de texto en diferentes posiciones.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-8 items-center justify-start">
      <Spinner label="Cargando..." labelPosition="right" variant="primary" />
      <Spinner label="Cargando..." labelPosition="left" variant="primary" />
      <Spinner label="Cargando..." labelPosition="top" variant="primary" />
      <Spinner label="Cargando..." labelPosition="bottom" variant="primary" />
    </div>
  }
>
```tsx
import { Spinner } from "@stofli/ui";

export default function SpinnerWithLabelDemo() {
  return (
    <div className="flex flex-wrap gap-8 items-center justify-start">
      <Spinner label="Cargando..." labelPosition="right" variant="primary" />
      <Spinner label="Cargando..." labelPosition="left" variant="primary" />
      <Spinner label="Cargando..." labelPosition="top" variant="primary" />
      <Spinner label="Cargando..." labelPosition="bottom" variant="primary" />
    </div>
  );
}
```
</DemoTabs>

### Combinaciones

Puedes combinar diferentes propiedades para personalizar el spinner según tus necesidades.

<DemoTabs
  preview={
    <div className="flex flex-wrap gap-8 items-center justify-start">
      <Spinner type="border" size="lg" variant="primary" label="Guardando..." labelPosition="right" />
      <Spinner type="dots" variant="success" label="Procesando..." labelPosition="bottom" />
      <Spinner type="grow" variant="danger" size="lg" speed="slow" />
    </div>
  }
>
```tsx
import { Spinner } from "@stofli/ui";

export default function SpinnerCombinationsDemo() {
  return (
    <div className="flex flex-wrap gap-8 items-center justify-start">
      <Spinner type="border" size="lg" variant="primary" label="Guardando..." labelPosition="right" />
      <Spinner type="dots" variant="success" label="Procesando..." labelPosition="bottom" />
      <Spinner type="grow" variant="danger" size="lg" speed="slow" />
    </div>
  );
}
```
</DemoTabs>

## Props

| Prop            | Tipo                                                                          | Default     | Descripción                            |
| --------------- | ----------------------------------------------------------------------------- | ----------- | -------------------------------------- |
| `size`          | `"xs" \| "sm" \| "md" \| "lg" \| "xl"`                                        | `"md"`      | Tamaño del spinner                     |
| `variant`       | `"default" \| "primary" \| "secondary" \| "success" \| "warning" \| "danger"` | `"default"` | Variante de color del spinner          |
| `speed`         | `"slow" \| "normal" \| "fast"`                                                | `"normal"`  | Velocidad de la animación              |
| `type`          | `"border" \| "dots" \| "grow" \| "wave" \| "lines"`                           | `"border"`  | Tipo de animación del spinner          |
| `label`         | `string`                                                                      | -           | Texto descriptivo para el spinner      |
| `labelPosition` | `"left" \| "right" \| "top" \| "bottom"`                                      | `"right"`   | Posición del texto respecto al spinner |
| `className`     | `string`                                                                      | -           | Clases adicionales para el componente  |
