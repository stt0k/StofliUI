---
title: "Pagination"
description: "Un componente de paginación para navegar a través de contenido dividido en páginas."
---

## Instalación

```bash
# Instalar dependencias necesarias
npm install framer-motion lucide-react

# Instalar el componente
npx @stofli/ui add pagination
```

## Uso

```tsx
import { Pagination } from "@stofli/ui";

export default function App() {
  return (
    <Pagination totalPages={10} currentPage={1} onPageChange={(page) => {}} />
  );
}
```

## Ejemplos

### Básico

Un componente de paginación básico con números de página y flechas de navegación.

<DemoTabs
  preview={
    <div className="w-full flex justify-center">
      <Pagination totalPages={10} currentPage={1} />
    </div>
  }
>
```tsx
import { Pagination } from "@stofli/ui";

export default function PaginationDemo() {
  return (
    <Pagination
      totalPages={10}
      currentPage={1}
      onPageChange={(page) => {}}
    />
  );
}
```
</DemoTabs>

### Variantes

El componente de paginación está disponible en diferentes variantes de color.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4 w-full">
      <Pagination totalPages={5} variant="default" />
      <Pagination totalPages={5} variant="primary" />
      <Pagination totalPages={5} variant="secondary" />
      <Pagination totalPages={5} variant="success" />
      <Pagination totalPages={5} variant="warning" />
      <Pagination totalPages={5} variant="danger" />
    </div>
  }
>
```tsx
import { Pagination } from "@stofli/ui";

export default function PaginationVariantsDemo() {
  return (
    <div className="flex flex-col gap-4 w-full">
      <Pagination totalPages={5} variant="default" />
      <Pagination totalPages={5} variant="primary" />
      <Pagination totalPages={5} variant="secondary" />
      <Pagination totalPages={5} variant="success" />
      <Pagination totalPages={5} variant="warning" />
      <Pagination totalPages={5} variant="danger" />
    </div>
  );
}
```
</DemoTabs>

### Tamaños

El componente de paginación está disponible en tres tamaños: sm, md y lg.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4 w-full">
      <Pagination totalPages={5} size="sm" />
      <Pagination totalPages={5} size="md" />
      <Pagination totalPages={5} size="lg" />
    </div>
  }
>
```tsx
import { Pagination } from "@stofli/ui";

export default function PaginationSizesDemo() {
  return (
    <div className="flex flex-col gap-4 w-full">
      <Pagination totalPages={5} size="sm" />
      <Pagination totalPages={5} size="md" />
      <Pagination totalPages={5} size="lg" />
    </div>
  );
}
```
</DemoTabs>

### Con botones de primera y última página

Usando `withEdges={true}`, muestra botones adicionales para ir a la primera y última página.

<DemoTabs
  preview={
    <div className="w-full flex justify-center">
      <Pagination totalPages={10} withEdges />
    </div>
  }
>
```tsx
import { Pagination } from "@stofli/ui";

export default function PaginationWithEdgesDemo() {
  return (
    <Pagination 
      totalPages={10} 
      withEdges 
      onPageChange={(page) => {}}
    />
  );
}
```
</DemoTabs>

### Con texto en los botones

Usando `withText={true}`, añade texto descriptivo a los botones de navegación.

<DemoTabs
  preview={
    <div className="w-full flex justify-center">
      <Pagination totalPages={10} withText />
    </div>
  }
>
```tsx
import { Pagination } from "@stofli/ui";

export default function PaginationWithTextDemo() {
  return (
    <Pagination 
      totalPages={10} 
      withText 
      onPageChange={(page) => {}}
    />
  );
}
```
</DemoTabs>

### Solo con flechas de navegación

Configurado con `withNumbers={false}` para mostrar solo los botones de navegación sin números de página.

<DemoTabs
  preview={
    <div className="w-full flex justify-center">
      <Pagination totalPages={10} withNumbers={false} />
    </div>
  }
>
```tsx
import { Pagination } from "@stofli/ui";

export default function PaginationArrowsOnlyDemo() {
  return (
    <Pagination 
      totalPages={10} 
      withNumbers={false} 
      onPageChange={(page) => {}}
    />
  );
}
```
</DemoTabs>

### Deshabilitado

Un componente de paginación deshabilitado que no permite interacción.

<DemoTabs
  preview={
    <div className="w-full flex justify-center">
      <Pagination totalPages={10} disabled />
    </div>
  }
>
```tsx
import { Pagination } from "@stofli/ui";

export default function PaginationDisabledDemo() {
  return (
    <Pagination 
      totalPages={10} 
      disabled 
      onPageChange={(page) => {}}
    />
  );
}
```
</DemoTabs>

## Props

| Prop            | Tipo                                                                          | Default     | Descripción                                          |
| --------------- | ----------------------------------------------------------------------------- | ----------- | ---------------------------------------------------- |
| `totalPages`    | `number`                                                                      | -           | Número total de páginas (requerido)                  |
| `currentPage`   | `number`                                                                      | `1`         | Página actual                                        |
| `onPageChange`  | `(page: number) => void`                                                      | -           | Función que se ejecuta al cambiar de página          |
| `siblingsCount` | `number`                                                                      | `1`         | Número de páginas adyacentes a mostrar               |
| `variant`       | `"default" \| "primary" \| "secondary" \| "success" \| "warning" \| "danger"` | `"default"` | Variante de color del componente                     |
| `size`          | `"sm" \| "md" \| "lg"`                                                        | `"md"`      | Tamaño del componente                                |
| `withNumbers`   | `boolean`                                                                     | `true`      | Mostrar números de página                            |
| `withEdges`     | `boolean`                                                                     | `false`     | Mostrar botones para ir a la primera y última página |
| `withText`      | `boolean`                                                                     | `false`     | Mostrar texto en los botones de navegación           |
| `disabled`      | `boolean`                                                                     | `false`     | Deshabilitar el componente                           |
| `className`     | `string`                                                                      | -           | Clases adicionales para el componente                |
