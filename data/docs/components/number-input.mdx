---
title: "Number Input"
description: "Un componente para entrada de valores numéricos con controles de incremento/decremento y animaciones."
---

## Instalación

```bash
# Instalar dependencias necesarias
npm install framer-motion lucide-react

# Instalar el componente
npx @stofli/ui add number-input
```

## Uso Básico

```jsx
import { NumberInput } from "@stofli/ui";

export default function App() {
  return <NumberInput label="Cantidad" defaultValue={10} min={0} max={100} />;
}
```

## Ejemplos

### Básico

<DemoTabs
  preview={
    <div className="flex flex-col gap-8 pt-8">
      <NumberInput label="Cantidad" defaultValue={10} min={0} max={100} />
    </div>
  }
>
```jsx
import { NumberInput } from "@stofli/ui";

export default function NumberInputDemo() {
  return <NumberInput label="Cantidad" defaultValue={10} min={0} max={100} />;
}
```
</DemoTabs>

### Variantes

El componente NumberInput ofrece distintas variantes para adaptarse a diferentes contextos visuales.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4 pt-8">
      <NumberInput variant="default" label="Default" defaultValue={5} />
      <NumberInput variant="primary" label="Primary" defaultValue={5} />
      <NumberInput variant="secondary" label="Secondary" defaultValue={5} />
      <NumberInput variant="success" label="Success" defaultValue={5} />
      <NumberInput variant="warning" label="Warning" defaultValue={5} />
      <NumberInput variant="danger" label="Danger" defaultValue={5} />
    </div>
  }
>
```jsx
import { NumberInput } from "@stofli/ui";

export default function NumberInputVariantsDemo() {
  return (
    <div className="flex flex-col gap-4">
      <NumberInput variant="default" label="Default" defaultValue={5} />
      <NumberInput variant="primary" label="Primary" defaultValue={5} />
      <NumberInput variant="secondary" label="Secondary" defaultValue={5} />
      <NumberInput variant="success" label="Success" defaultValue={5} />
      <NumberInput variant="warning" label="Warning" defaultValue={5} />
      <NumberInput variant="danger" label="Danger" defaultValue={5} />
    </div>
  );
}
```
</DemoTabs>

### Tamaños

Puedes elegir entre diferentes tamaños para el input numérico.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4 pt-8">
      <NumberInput size="sm" label="Pequeño" defaultValue={5} />
      <NumberInput size="md" label="Mediano" defaultValue={5} />
      <NumberInput size="lg" label="Grande" defaultValue={5} />
    </div>
  }
>
```jsx
import { NumberInput } from "@stofli/ui";

export default function NumberInputSizesDemo() {
  return (
    <div className="flex flex-col gap-4">
      <NumberInput size="sm" label="Pequeño" defaultValue={5} />
      <NumberInput size="md" label="Mediano" defaultValue={5} />
      <NumberInput size="lg" label="Grande" defaultValue={5} />
    </div>
  );
}
```
</DemoTabs>

### Radios de borde

Puedes personalizar el radio de los bordes del input para adaptarlo a tu diseño.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4 pt-8">
      <NumberInput 
        label="Sin bordes redondeados" 
        radius="none"
        defaultValue={5}
        variant="primary"
      />
      <NumberInput 
        label="Radio pequeño"
        radius="sm"
        defaultValue={5}
        variant="secondary"
      />
      <NumberInput 
        label="Radio mediano (predeterminado)"
        radius="md"
        defaultValue={5}
        variant="success"
      />
      <NumberInput 
        label="Radio grande"
        radius="full"
        defaultValue={5}
        variant="warning"
      />
    </div>
  }
>
```jsx
import { NumberInput } from "@stofli/ui";

export default function NumberInputRadiusDemo() {
  return (
    <div className="flex flex-col gap-4">
      <NumberInput 
        label="Sin bordes redondeados" 
        radius="none"
        defaultValue={5}
        variant="primary"
      />
      <NumberInput 
        label="Radio pequeño"
        radius="sm"
        defaultValue={5}
        variant="secondary"
      />
      <NumberInput 
        label="Radio mediano (predeterminado)"
        radius="md"
        defaultValue={5}
        variant="success"
      />
      <NumberInput 
        label="Radio grande"
        radius="full"
        defaultValue={5}
        variant="warning"
      />
    </div>
  );
}
```
</DemoTabs>

### Posición de los controles

Puedes elegir diferentes posiciones para los controles de incremento/decremento.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4 pt-8">
      <NumberInput controlsPosition="right" label="Controles a la derecha" defaultValue={5} />
      <NumberInput controlsPosition="both" label="Controles en ambos lados" defaultValue={5} />
    </div>
  }
>
```jsx
import { NumberInput } from "@stofli/ui";

export default function NumberInputControlsPositionDemo() {
  return (
    <div className="flex flex-col gap-4">
      <NumberInput controlsPosition="right" label="Controles a la derecha" defaultValue={5} />
      <NumberInput controlsPosition="both" label="Controles en ambos lados" defaultValue={5} />
    </div>
  );
}
```
</DemoTabs>

### Layout de controles

El componente ofrece diferentes estilos de layout para los controles.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4 pt-8">
      <NumberInput controlsLayout="default" label="Layout predeterminado" defaultValue={5} />
      <NumberInput controlsLayout="compact" label="Layout compacto" defaultValue={5} />
    </div>
  }
>
```jsx
import { NumberInput } from "@stofli/ui";

export default function NumberInputControlsLayoutDemo() {
  return (
    <div className="flex flex-col gap-4">
      <NumberInput controlsLayout="default" label="Layout predeterminado" defaultValue={5} />
      <NumberInput controlsLayout="compact" label="Layout compacto" defaultValue={5} />
    </div>
  );
}
```
</DemoTabs>

### Conjunto de iconos

Puedes elegir entre diferentes estilos de iconos para los controles.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4 pt-8">
      <NumberInput iconSet="chevron" label="Iconos chevron" defaultValue={5} />
      <NumberInput iconSet="plusminus" label="Iconos plus/minus" defaultValue={5} />
    </div>
  }
>
```jsx
import { NumberInput } from "@stofli/ui";

export default function NumberInputIconSetsDemo() {
  return (
    <div className="flex flex-col gap-4">
      <NumberInput iconSet="chevron" label="Iconos chevron" defaultValue={5} />
      <NumberInput iconSet="plusminus" label="Iconos plus/minus" defaultValue={5} />
    </div>
  );
}
```
</DemoTabs>

### Estilos de animación

El componente incluye diferentes estilos de animación cuando el valor cambia.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4 pt-8">
      <NumberInput animationStyle="slide" label="Animación deslizar" defaultValue={5} />
      <NumberInput animationStyle="pulse" label="Animación pulso" defaultValue={5} />
      <NumberInput animationStyle="scale" label="Animación escala" defaultValue={5} />
      <NumberInput animationStyle="bounce" label="Animación rebote" defaultValue={5} />
    </div>
  }
>
```jsx
import { NumberInput } from "@stofli/ui";

export default function NumberInputAnimationsDemo() {
  return (
    <div className="flex flex-col gap-4">
      <NumberInput animationStyle="slide" label="Animación deslizar" defaultValue={5} />
      <NumberInput animationStyle="pulse" label="Animación pulso" defaultValue={5} />
      <NumberInput animationStyle="scale" label="Animación escala" defaultValue={5} />
      <NumberInput animationStyle="bounce" label="Animación rebote" defaultValue={5} />
    </div>
  );
}
```
</DemoTabs>

### Con límites y paso

Puedes establecer valores mínimos y máximos, así como el paso de incremento/decremento.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4 pt-8">
      <NumberInput 
        label="Temperatura (°C)" 
        defaultValue={25} 
        min={-20} 
        max={50} 
        step={0.5} 
      />
    </div>
  }
>
```jsx
import { NumberInput } from "@stofli/ui";

export default function NumberInputLimitsDemo() {
  return (
    <NumberInput 
      label="Temperatura (°C)" 
      defaultValue={25} 
      min={-20} 
      max={50} 
      step={0.5} 
    />
  );
}
```
</DemoTabs>

### Deshabilitado y solo lectura

<DemoTabs
  preview={
    <div className="flex flex-col gap-4 pt-8">
      <NumberInput 
        label="Deshabilitado" 
        defaultValue={42} 
        disabled 
      />
      <NumberInput 
        label="Solo lectura" 
        defaultValue={42} 
        readonly 
      />
    </div>
  }
>
```jsx
import { NumberInput } from "@stofli/ui";

export default function NumberInputDisabledDemo() {
  return (
    <div className="flex flex-col gap-4">
      <NumberInput 
        label="Deshabilitado" 
        defaultValue={42} 
        disabled 
      />
      <NumberInput 
        label="Solo lectura" 
        defaultValue={42} 
        readonly 
      />
    </div>
  );
}
```
</DemoTabs>

### Con mensaje de error

Muestra un mensaje de error cuando se cumple una condición, en este caso, cuando el valor es negativo.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4 pt-8">
      <NumberInput 
        label="Precio" 
        defaultValue={-10} 
        error={-10 < 0 ? "El precio no puede ser negativo" : undefined} 
      />
    </div>
  }
>
```jsx
import { NumberInput } from "@stofli/ui";
import { useState } from "react";

export default function NumberInputErrorDemo() {
  const [value, setValue] = useState(-10);
  
  return (
    <NumberInput 
      label="Precio" 
      value={value}
      onChange={setValue}
      error={value < 0 ? "El precio no puede ser negativo" : undefined}
    />
  );
}
```
</DemoTabs>

### Ancho completo

<DemoTabs
  preview={
    <div className="flex flex-col gap-4 pt-8">
      <NumberInput 
        label="Cantidad" 
        defaultValue={5} 
        fullWidth 
      />
    </div>
  }
>
```jsx
import { NumberInput } from "@stofli/ui";

export default function NumberInputFullWidthDemo() {
  return (
    <NumberInput 
      label="Cantidad" 
      defaultValue={5} 
      fullWidth 
    />
  );
}
```
</DemoTabs>

### Componente controlado

Ejemplo de uso con un componente controlado donde se puede obtener el valor en tiempo real.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4 pt-8">
      <div className="flex flex-col gap-2">
        <NumberInputControlledDemo />
      </div>
    </div>
  }
>
```jsx
import { NumberInput } from "@stofli/ui";
import { useState } from "react";

export default function NumberInputControlledDemo() {
  const [value, setValue] = useState(10);
  
  return (
    <>
      <NumberInput 
        label="Valor controlado" 
        value={value}
        onChange={(newValue) => setValue(newValue)}
      />
      <p className="text-sm mt-2">Valor actual: <span className="font-semibold">{value}</span></p>
    </>
  );
}
```
</DemoTabs>

### Ejemplos con formato

Ejemplos de NumberInput con diferentes formatos incorporados directamente en el componente.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4 pt-8">
      <NumberInput 
        label="Porcentaje" 
        defaultValue={25} 
        min={0}
        max={100}
        format="percentage"
        formatOptions={{ position: "suffix" }}
        variant="primary"
      />
      <NumberInput 
        label="Precio (€)" 
        defaultValue={99.95} 
        min={0}
        step={0.01}
        format="currency"
        formatOptions={{ symbol: "€", position: "suffix" }}
        variant="success"
      />
      <NumberInput 
        label="Valor con signo" 
        defaultValue={42} 
        format="signed"
        variant="secondary"
      />
    </div>
  }
>
```jsx
import { NumberInput } from "@stofli/ui";

export default function NumberInputFormatsDemo() {
  return (
    <div className="flex flex-col gap-4">
      <NumberInput 
        label="Porcentaje" 
        defaultValue={25} 
        min={0}
        max={100}
        format="percentage"
        formatOptions={{ position: "suffix" }}
        variant="primary"
      />
      <NumberInput 
        label="Precio (€)" 
        defaultValue={99.95} 
        min={0}
        step={0.01}
        format="currency"
        formatOptions={{ symbol: "€", position: "suffix" }}
        variant="success"
      />
      <NumberInput 
        label="Valor con signo" 
        defaultValue={42} 
        format="signed"
        variant="secondary"
      />
    </div>
  );
}
```
</DemoTabs>

### Valores mínimos y máximos

Ejemplos de uso con valores mínimos y máximos configurados.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4 pt-8">
      <NumberInput
        label="Rango limitado (0-100)"
        defaultValue={50}
        min={0}
        max={100}
        variant="primary"
      />
    </div>
  }
>
```jsx
import { NumberInput } from "@stofli/ui";

export default function NumberInputMinMaxDemo() {
  return (
    <div className="flex flex-col gap-4">
      <NumberInput
        label="Rango limitado (0-100)"
        defaultValue={50}
        min={0}
        max={100}
        variant="primary"
      />
    </div>
  );
}
```
</DemoTabs>

### Con placeholders

Los placeholders ayudan a los usuarios a entender qué tipo de información deben ingresar en el campo.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4 pt-8">
      <NumberInput 
        label="Edad"
        placeholder="Ingresa tu edad" 
        variant="primary"
      />
      <NumberInput 
        label="Temperatura"
        placeholder="Ej: 25.5" 
        variant="warning"
      />
      <NumberInput 
        label="Precio (€)"
        placeholder="0.00" 
        format="currency"
        formatOptions={{ symbol: "€", position: "suffix" }}
        variant="success"
      />
      <NumberInput 
        placeholder="Sin etiqueta, solo placeholder" 
        variant="secondary"
      />
    </div>
  }
>
```jsx
import { NumberInput } from "@stofli/ui";

export default function NumberInputPlaceholderDemo() {
  return (
    <div className="flex flex-col gap-4">
      <NumberInput 
        label="Edad"
        placeholder="Ingresa tu edad" 
        variant="primary"
      />
      <NumberInput 
        label="Temperatura"
        placeholder="Ej: 25.5" 
        variant="warning"
      />
      <NumberInput 
        label="Precio (€)"
        placeholder="0.00" 
        format="currency"
        formatOptions={{ symbol: "€", position: "suffix" }}
        variant="success"
      />
      <NumberInput 
        placeholder="Sin etiqueta, solo placeholder" 
        variant="secondary"
      />
    </div>
  );
}
```
</DemoTabs>

### Sin botones de control

Puedes ocultar los botones de incremento/decremento para obtener un input numérico más sencillo.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4 pt-8">
      <NumberInput 
        label="Input numérico simple" 
        placeholder="Sin botones" 
        showControls={false}
        variant="primary"
      />
    </div>
  }
>
```jsx
import { NumberInput } from "@stofli/ui";

export default function NumberInputNoControlsDemo() {
  return (
    <NumberInput 
      label="Input numérico simple" 
      placeholder="Sin botones" 
      showControls={false}
      variant="primary"
    />
  );
}
```
</DemoTabs>

### Campos requeridos

Usa la propiedad `required` para marcar un campo como obligatorio, lo que añadirá un asterisco al label.

<DemoTabs
  preview={
    <div className="flex flex-col gap-4 pt-8">
      <NumberInput 
        label="Campo obligatorio" 
        required
        variant="primary"
      />
      <NumberInput 
        label="Campo opcional"
        variant="primary"
      />
    </div>
  }
>
```jsx
import { NumberInput } from "@stofli/ui";

export default function NumberInputRequiredDemo() {
  return (
    <div className="flex flex-col gap-4">
      <NumberInput 
        label="Campo obligatorio" 
        required
        variant="primary"
      />
      <NumberInput 
        label="Campo opcional"
        variant="primary"
      />
    </div>
  );
}
```
</DemoTabs>

## Props

| Prop               | Tipo                                                                          | Valor por defecto | Descripción                                           |
| ------------------ | ----------------------------------------------------------------------------- | ----------------- | ----------------------------------------------------- |
| `value`            | `number`                                                                      | -                 | Valor controlado del input numérico                   |
| `defaultValue`     | `number`                                                                      | `0`               | Valor inicial no controlado                           |
| `min`              | `number`                                                                      | -                 | Valor mínimo permitido                                |
| `max`              | `number`                                                                      | -                 | Valor máximo permitido                                |
| `step`             | `number`                                                                      | `1`               | Incremento por cada pulsación en los controles        |
| `disabled`         | `boolean`                                                                     | `false`           | Deshabilita el input                                  |
| `readonly`         | `boolean`                                                                     | `false`           | Modo solo lectura                                     |
| `label`            | `string`                                                                      | -                 | Etiqueta descriptiva para el input                    |
| `placeholder`      | `string`                                                                      | -                 | Texto de marcador de posición                         |
| `error`            | `string`                                                                      | -                 | Mensaje de error a mostrar                            |
| `size`             | `"sm" \| "md" \| "lg"`                                                        | `"md"`            | Tamaño del componente                                 |
| `variant`          | `"default" \| "primary" \| "secondary" \| "success" \| "warning" \| "danger"` | `"default"`       | Variante de estilo                                    |
| `radius`           | `"none" \| "sm" \| "md" \| "full"`                                            | `"md"`            | Radio de las esquinas                                 |
| `fullWidth`        | `boolean`                                                                     | `false`           | Ocupa el 100% del ancho disponible                    |
| `className`        | `string`                                                                      | -                 | Clases CSS personalizadas para el contenedor          |
| `inputClassName`   | `string`                                                                      | -                 | Clases CSS personalizadas para el input               |
| `controlsLayout`   | `"default" \| "compact" \| "split"`                                           | `"default"`       | Diseño de los controles                               |
| `controlsPosition` | `"right" \| "both"`                                                           | `"right"`         | Posición de los controles                             |
| `showControls`     | `boolean`                                                                     | `true`            | Muestra u oculta los controles                        |
| `iconSet`          | `"chevron" \| "plusminus"`                                                    | `"chevron"`       | Conjunto de iconos a utilizar                         |
| `required`         | `boolean`                                                                     | `false`           | Marca el campo como obligatorio                       |
| `name`             | `string`                                                                      | -                 | Nombre del campo (para formularios)                   |
| `id`               | `string`                                                                      | -                 | ID único del elemento                                 |
| `animationStyle`   | `"slide" \| "pulse" \| "scale" \| "bounce"`                                   | `"slide"`         | Estilo de animación al cambiar valor                  |
| `format`           | `"default" \| "percentage" \| "currency" \| "signed"`                         | `"default"`       | Formato de visualización del valor                    |
| `formatOptions`    | `{ symbol?: string; position?: "prefix" \| "suffix" }`                        | `{}`              | Opciones adicionales para el formato seleccionado     |
| `onChange`         | `(value: number) => void`                                                     | -                 | Función que se ejecuta al cambiar el valor            |
| `onFocus`          | `(e: React.FocusEvent) => void`                                               | -                 | Función que se ejecuta cuando el input recibe el foco |
| `onBlur`           | `(e: React.FocusEvent) => void`                                               | -                 | Función que se ejecuta cuando el input pierde el foco |
