---
title: "Tabs"
description: "Un componente de pestañas con animaciones suaves y opciones de personalización."
---

## Instalación

```bash
# Instalar dependencias necesarias
npm install framer-motion

# Instalar el componente
npx @stofli/ui add tabs
```

## Uso

```tsx
import { Tabs } from "@stofli/ui";

export default function App() {
  return (
    <Tabs
      tabs={[
        {
          label: "Pestaña 1",
          content: <p>Contenido de la pestaña 1</p>,
        },
        {
          label: "Pestaña 2",
          content: <p>Contenido de la pestaña 2</p>,
        },
      ]}
    />
  );
}
```

## Ejemplos

### Básico

Muestra un componente de pestañas básico.

<DemoTabs
  preview={
    <Tabs
      tabs={[
        {
          label: "Cuenta",
          content: <p className="text-zinc-600 dark:text-zinc-400">Configuración de tu cuenta y preferencias.</p>
        },
        {
          label: "Contraseña",
          content: <p className="text-zinc-600 dark:text-zinc-400">Gestiona la seguridad de tu cuenta.</p>
        },
        {
          label: "Notificaciones",
          content: <p className="text-zinc-600 dark:text-zinc-400">Configura cómo y cuándo recibir alertas.</p>
        }
      ]}
    />
  }
>
```tsx
import { Tabs } from "@stofli/ui";

export default function TabsDemo() {
  return (
    <Tabs
      tabs={[
        {
          label: "Cuenta",
          content: <p className="text-zinc-600 dark:text-zinc-400">Configuración de tu cuenta y preferencias.</p>
        },
        {
          label: "Contraseña",
          content: <p className="text-zinc-600 dark:text-zinc-400">Gestiona la seguridad de tu cuenta.</p>
        },
        {
          label: "Notificaciones",
          content: <p className="text-zinc-600 dark:text-zinc-400">Configura cómo y cuándo recibir alertas.</p>
        }
      ]}
    />
  );
}
```
</DemoTabs>

### Con íconos

Añade íconos a las pestañas.

<DemoTabs
  preview={
    <Tabs
      tabs={[
        {
          label: "Perfil",
          icon: <UserIcon className="h-4 w-4" />,
          content: <p className="text-zinc-600 dark:text-zinc-400">Información de tu perfil de usuario.</p>
        },
        {
          label: "Mensajes",
          icon: <MessageSquareIcon className="h-4 w-4" />,
          content: <p className="text-zinc-600 dark:text-zinc-400">Bandeja de entrada y mensajes.</p>
        },
        {
          label: "Ajustes",
          icon: <SettingsIcon className="h-4 w-4" />,
          content: <p className="text-zinc-600 dark:text-zinc-400">Configuración de la aplicación.</p>
        }
      ]}
    />
  }
>
```tsx
import { Tabs } from "@stofli/ui";
import { UserIcon, MessageSquareIcon, SettingsIcon } from "lucide-react";

export default function TabsWithIconsDemo() {
  return (
    <Tabs
      tabs={[
        {
          label: "Perfil",
          icon: <UserIcon className="h-4 w-4" />,
          content: <p className="text-zinc-600 dark:text-zinc-400">Información de tu perfil de usuario.</p>
        },
        {
          label: "Mensajes",
          icon: <MessageSquareIcon className="h-4 w-4" />,
          content: <p className="text-zinc-600 dark:text-zinc-400">Bandeja de entrada y mensajes.</p>
        },
        {
          label: "Ajustes",
          icon: <SettingsIcon className="h-4 w-4" />,
          content: <p className="text-zinc-600 dark:text-zinc-400">Configuración de la aplicación.</p>
        }
      ]}
    />
  );
}
```
</DemoTabs>

### Con efecto hover

Usando `hoverEffect={true}` para mostrar una animación al pasar el ratón por encima de las pestañas.

<DemoTabs
  preview={
    <Tabs
      hoverEffect
      tabs={[
        {
          label: "Producto",
          content: <p className="text-zinc-600 dark:text-zinc-400">Información sobre el producto.</p>
        },
        {
          label: "Servicios",
          content: <p className="text-zinc-600 dark:text-zinc-400">Servicios disponibles.</p>
        },
        {
          label: "Contenido",
          content: <p className="text-zinc-600 dark:text-zinc-400">Contenido personalizado.</p>
        },
        {
          label: "Playground",
          content: <p className="text-zinc-600 dark:text-zinc-400">Espacio para experimentar con el producto.</p>
        }
      ]}
    />
  }
>
```tsx
import { Tabs } from "@stofli/ui";

export default function TabsHoverEffectDemo() {
  return (
    <Tabs
      hoverEffect
      tabs={[
        {
          label: "Producto",
          content: <p className="text-zinc-600 dark:text-zinc-400">Información sobre el producto.</p>
        },
        {
          label: "Servicios",
          content: <p className="text-zinc-600 dark:text-zinc-400">Servicios disponibles.</p>
        },
        {
          label: "Contenido",
          content: <p className="text-zinc-600 dark:text-zinc-400">Contenido personalizado.</p>
        },
        {
          label: "Playground",
          content: <p className="text-zinc-600 dark:text-zinc-400">Espacio para experimentar con el producto.</p>
        }
      ]}
    />
  );
}
```
</DemoTabs>

### Variantes

Usando la prop `variant`: default, primary, secondary, success, warning y danger.

<DemoTabs
  preview={
    <div className="flex flex-col gap-6">
      <Tabs
        variant="default"
        tabs={[
          { label: "Default", content: <p>Variante predeterminada</p> },
          { label: "Tab 2", content: <p>Contenido 2</p> }
        ]}
      />
      <Tabs
        variant="primary"
        tabs={[
          { label: "Primary", content: <p>Variante principal</p> },
          { label: "Tab 2", content: <p>Contenido 2</p> }
        ]}
      />
      <Tabs
        variant="secondary"
        tabs={[
          { label: "Secondary", content: <p>Variante secundaria</p> },
          { label: "Tab 2", content: <p>Contenido 2</p> }
        ]}
      />
      <Tabs
        variant="success"
        tabs={[
          { label: "Success", content: <p>Variante de éxito</p> },
          { label: "Tab 2", content: <p>Contenido 2</p> }
        ]}
      />
      <Tabs
        variant="warning"
        tabs={[
          { label: "Warning", content: <p>Variante de advertencia</p> },
          { label: "Tab 2", content: <p>Contenido 2</p> }
        ]}
      />
      <Tabs
        variant="danger"
        tabs={[
          { label: "Danger", content: <p>Variante de peligro</p> },
          { label: "Tab 2", content: <p>Contenido 2</p> }
        ]}
      />
    </div>
  }
>
```tsx
import { Tabs } from "@stofli/ui";

export default function TabsVariantsDemo() {
  return (
    <div className="flex flex-col gap-6">
      <Tabs
        variant="default"
        tabs={[
          { label: "Default", content: <p>Variante predeterminada</p> },
          { label: "Tab 2", content: <p>Contenido 2</p> }
        ]}
      />
      <Tabs
        variant="primary"
        tabs={[
          { label: "Primary", content: <p>Variante principal</p> },
          { label: "Tab 2", content: <p>Contenido 2</p> }
        ]}
      />
      {/* ... otras variantes ... */}
    </div>
  );
}
```
</DemoTabs>

### Tamaños

Usando la prop `size`: sm, md y lg.

<DemoTabs
  preview={
    <div className="flex flex-col gap-6">
      <Tabs
        size="sm"
        tabs={[
          { label: "Pequeño", content: <p>Tabs de tamaño pequeño</p> },
          { label: "Tab 2", content: <p>Contenido 2</p> }
        ]}
      />
      <Tabs
        size="md"
        tabs={[
          { label: "Mediano", content: <p>Tabs de tamaño mediano</p> },
          { label: "Tab 2", content: <p>Contenido 2</p> }
        ]}
      />
      <Tabs
        size="lg"
        tabs={[
          { label: "Grande", content: <p>Tabs de tamaño grande</p> },
          { label: "Tab 2", content: <p>Contenido 2</p> }
        ]}
      />
    </div>
  }
>
```tsx
import { Tabs } from "@stofli/ui";

export default function TabsSizesDemo() {
  return (
    <div className="flex flex-col gap-6">
      <Tabs
        size="sm"
        tabs={[
          { label: "Pequeño", content: <p>Tabs de tamaño pequeño</p> },
          { label: "Tab 2", content: <p>Contenido 2</p> }
        ]}
      />
      <Tabs
        size="md"
        tabs={[
          { label: "Mediano", content: <p>Tabs de tamaño mediano</p> },
          { label: "Tab 2", content: <p>Contenido 2</p> }
        ]}
      />
      <Tabs
        size="lg"
        tabs={[
          { label: "Grande", content: <p>Tabs de tamaño grande</p> },
          { label: "Tab 2", content: <p>Contenido 2</p> }
        ]}
      />
    </div>
  );
}
```
</DemoTabs>

### Anchura completa

Usando la prop `fullWidth` para expandir las pestañas al ancho completo del contenedor.

<DemoTabs
  preview={
    <Tabs
      fullWidth
      tabs={[
        { label: "Sección 1", content: <p>Pestañas con ancho completo</p> },
        { label: "Sección 2", content: <p>Contenido 2</p> },
        { label: "Sección 3", content: <p>Contenido 3</p> }
      ]}
    />
  }
>
```tsx
import { Tabs } from "@stofli/ui";

export default function TabsFullWidthDemo() {
  return (
    <Tabs
      fullWidth
      tabs={[
        { label: "Sección 1", content: <p>Pestañas con ancho completo</p> },
        { label: "Sección 2", content: <p>Contenido 2</p> },
        { label: "Sección 3", content: <p>Contenido 3</p> }
      ]}
    />
  );
}
```
</DemoTabs>

## Props

| Prop               | Tipo                                                                          | Default     | Descripción                                     |
| ------------------ | ----------------------------------------------------------------------------- | ----------- | ----------------------------------------------- |
| `tabs`             | `Array<{ label: string, content: ReactNode, icon?: ReactNode }>`              | -           | Lista de pestañas con su contenido              |
| `defaultTab`       | `number`                                                                      | `0`         | Índice de la pestaña activa por defecto         |
| `variant`          | `"default" \| "primary" \| "secondary" \| "success" \| "warning" \| "danger"` | `"default"` | Variante de estilo                              |
| `size`             | `"sm" \| "md" \| "lg"`                                                        | `"md"`      | Tamaño de las pestañas                          |
| `radius`           | `"none" \| "sm" \| "md" \| "full"`                                            | `"md"`      | Radio de las esquinas                           |
| `fullWidth`        | `boolean`                                                                     | `false`     | Si las pestañas ocupan todo el ancho disponible |
| `hoverEffect`      | `boolean`                                                                     | `false`     | Activa el efecto de hover en las pestañas       |
| `className`        | `string`                                                                      | -           | Clases adicionales para el contenedor           |
| `tabClassName`     | `string`                                                                      | -           | Clases adicionales para cada pestaña            |
| `contentClassName` | `string`                                                                      | -           | Clases adicionales para el contenido            |
